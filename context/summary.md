# Контекст — точка входа | Жемчужина

> Последнее обновление: 05.08.2026
> Правило: только текущее состояние. История — в logs/terminal/

## Где остановились

**Сессия 05.08, продолжение 3 — коэффициент садик/школа реализован и задеплоен, закрыт открытый пункт с 04.08:**

1. **Разрыв 04.08 закрыт.** Экран рецепта (`/menu/dishes/{id}`) теперь
   просит только «Школа, г» — садик считается сам, живым коэффициентом
   (`Organization.sadik_portion_percent`, default 80%, редактируется на
   `/services/`), не хранится в БД. Штучная выпечка (21 блюдо — Булочка,
   Кекс, Пирог и т.п.) — чекбокс «одинаковая порция» на уровне блюда,
   даёт 100% вместо коэффициента. Перед кодом сверил решение с реальными
   510 строками рецептов — 0 необъяснённых отклонений, ровно 21 «штучное»
   блюдо, ни одного смешанного. После деплоя прогнал эквивалентность на
   реальном дне меню прода (21 товар) — старая и новая формула совпали
   до грамма. Подробности — [[architecture/menu_module]].
2. **Архитектурное решение**: садик — не хранимый снимок, а живой
   расчёт в `writeoff_calc.compute_day_draft` (тот же урок, что и с
   переходным тарифом billing.py) — иначе Садик Кожомкул (уже есть в
   базе, id=5) не смог бы иметь свой процент отдельно от Сокулука.
3. **Кнопка «↻ Повторить неделю» на `/menu/`** — по прямому запросу
   (Махабат каждую неделю вручную вбивает почти то же самое заново).
   `POST /menu/copy-week` копирует блюда с предыдущей недели **только в
   пустые дни текущей** — уже заполненные не трогает и не задваивает
   (проверено вживую: повторный клик на уже скопированной неделе честно
   сообщает «копировать нечего», не создаёт дублей). Копируются только
   `dish_id`, у которых уже был рецепт на момент ввода в прошлую неделю —
   то же условие, что и так стоит на вводе меню, дополнительно ничего не
   проверялось.

---

**Сессия 05.08 (продолжение) — июльские начисления удалены на проде + фича «создать товар из рецепта» реализована и проверена локально, НЕ задеплоена:**

1. **Июльские начисления удалены** (закрыт открытый вопрос с 04.08) — см.
   раздел ниже «Сессия 04.08» и [[architecture/billing_module]]. Родители
   в июле платили вне ИС, боевой отсчёт — с 1 августа. 123 строки `Charge`
   (842 258.10 сом) удалены прямым SQL на проде после подтверждения
   владельцем, before/after показан.
2. **Фича «+ создать товар прямо из рецепта» реализована** (план был
   согласован 30-31.07, сегодня уточнён через advisor и построен) — новый
   роут `POST /expenses/products/create`, модалка на `dish_detail.html`,
   двухслойный дедуп (дропдаун + серверная fuzzy-проверка ≥85,
   `standard_only=False` на обоих слоях), `grams_per_unit` обязателен
   условно для шт/уп/пач. Полная методика и решения по развилкам —
   [[architecture/menu_module]]. **Проверено вживую локально** (Playwright
   недоступен, ходил через claude-in-chrome по реальным экранам:
   happy path создания + дедуп-предупреждение с игнорированием), `pytest`
   — без новых регрессий (те же 2 доеденных падения в `test_products.py`,
   подтверждено сравнением до/после через `git stash`). Тестовые данные из
   локальной БД почищены после проверки.
3. **✅ Задеплоено** — коммит `c251d27` запушен на `master`, Railway
   подтвердил `SUCCESS`, логи чистые (миграция не требовалась — новых
   колонок в модели `Product` нет).
4. Заодно на глаза попался **отдельный вероятный баг** (не трогал,
   не проверял глубоко): `app/routers/expenses.py:39` —
   `PILOT_USER_IDS = {1, 3, 61, 64}` — та же константа, что чинили 14.07
   в `dependencies.py` (правильные id Айдай/Талас — 5/6, не 61/64), но
   копия в `expenses.py` (комментарий в коде сам называет её известным
   техдолгом-дубликатом) осталась со старыми id. Эффект: в модуле Расходы
   `get_accessible_orgs()` для Айдай/Талас не сработает pilot-ограничение
   («только Садик Сокулук») — они попадут в ветку `role in (owner,...)` и
   увидят все объекты. Не критично (это семья, совладельцы), но расходится
   с осознанным решением 13.07. Стоит поправить в одну строку, когда будет
   минутка.
5. **🔴 Найден и исправлен реальный денежный баг — задвоенные/затроенные
   карточки детей** (не гонка начислений, а гонка **создания ребёнка**,
   тот же класс TOCTOU, что уже чинили в billing 04.08 и menu 23.07, но
   для `/students/add` защиты ещё нет). Обнаружено по конкретному
   запросу владельца (Бопоева Акмаанай), запрос по всей базе
   (`GROUP BY lower(name), org HAVING count(*)>1`) нашёл **8 групп**, 18
   строк вместо 8 детей:
   - **Бопоева Акмаанай** (3 карточки, разница 3 секунды) — реальный
     платёж 8000 сом Optima ушёл на PIN неактивной карточки-дубля,
     из-за чего этот PIN уже отказывал бы банку при повторной попытке.
     Начисление также успело посчитаться до появления Enrollment →
     не увидело льготный тариф (10000 вместо 8000). Исправлено: PIN
     0132 перенесён на карточку с правильным Enrollment, начисление
     поправлено на 8000, платёж перепривязан. **Балансы других детей на
     `/reports/debtors` эта правка не пересчитывает автоматически** —
     они и так считались верно, только у Бопоевой цифры поменяются
     при следующем заходе.
   - **Ташболотов Даниэл** (2 карточки, обе активны) и **Шахтыбеков
     Баатырхан** (3 карточки, все активны) — оба **реально задваивали/
     затраивали показанный долг живьём** (начисление на каждую
     активную копию). Исправлено — оставлена по одной активной
     карточке (та, где реальные деньги/история), дубли деактивированы,
     их августовские начисления удалены.
   - **Максудбекова Хадижа** и **Тилебалдиева Нуркыз** — обе карточки
     были неактивны (Enrollment закрыт у обеих, видимо кто-то путался
     при переводе между группами). Владелец подтвердил — дети всё ещё
     ходят, реактивированы (новый открытый `Enrollment` с сегодня),
     начисление за август посчитается само при следующем заходе на
     `/students/`.
   - **Анваров Ахмад, Кубатбекова Айтунук, Садырбеков Абдурахим** —
     дубли уже неактивны и пусты (без начислений/оплат) — реального
     вреда не было, действий не потребовалось. У Анварова отдельно
     замечено: посещаемость 22–23.07 отмечена на ОБЕИХ карточках (было
     две разных группы) — не трогал, задним числом на списание/выручку
     не влияет, исторический артефакт.
   - Полная SQL-диагностика и фиксы — только в этой сессии, wiki-запись
     не заведена (специфика этого дня, не общий паттерн модуля).
6. **В бэклог, не сделано**: защита от двойного/тройного сабмита —
   по прямому запросу владельца, глобально по системе, начиная как
   минимум с `/students/add` (та же гонка, что породила пункт 5).

**Первым делом в следующей сессии:**
1. Решить вариант А или Б для коэффициента садик/школа×0.80 на экране
   рецепта (см. [[architecture/menu_module]])
2. Уточнить у Махабат по «Филе грудки птицы (мяготь)» — теперь, когда
   фича готова, можно завести как новый товар прямо на месте, если она
   подтвердит, что это не алиас
3. Защита от двойного/тройного сабмита при создании ребёнка (см. п.6
   выше) — начать с `/students/add`, по аналогии с advisory lock в
   `generate_monthly_charges`/`menu_day_save`
4. Мелкий техдолг: `expenses.py:39` — устаревшие `PILOT_USER_IDS`, см. п.4
5. Продолжают числиться с прошлых сессий: 10 найденных дублей блюд,
   тестовые PIN 9001/9002, `txn_date` парсинг Optima, Соцфонд 12.25%,
   наполнение `/services/`, реестр сотрудников

---

**Сессия 04.08 (живая проверка с Махабат/Мунарой нашла реальный денежный баг — гонка задвоила начисления, 1.6млн сом фантомного долга) — 3 деплоя за сессию:**

1. **Найден и исправлен серьёзный баг биллинга.** `generate_monthly_charges()`
   не имела блокировки между проверкой «уже начислено?» и вставкой — 1
   августа 5 запросов подряд за 24 секунды задвоили начисления 50 из 60
   детей (по 5 копий каждому вместо одной). Диагностировано через прямое
   чтение прод-БД (Railway), показан owner'у полный before/after diff по
   всем 50 детям до удаления. Реальных денег не затронуло (`Transaction`
   не трогался) — только показанный долг был завышен. Подробности —
   [[architecture/billing_module]].
2. **Три деплоя-фикса:** (1) `pg_advisory_xact_lock` в
   `generate_monthly_charges` — закрывает саму гонку; (2) частичный
   уникальный индекс `charges(student_id, date) WHERE description=
   'Начисление за месяц'` — защита на уровне схемы; (3) `get_balance`/
   `get_balances` теперь фильтруют `Transaction.deleted_at` (латентный
   баг, на проде пока 0 удалённых оплат — не проявлялся, но чинился заодно).
3. **Данные очищены**: 200 лишних строк начислений удалены на проде (диф
   показан и подтверждён владельцем до удаления). Июльские начисления
   проверены отдельно — чистые, баг их не касался.
4. **Разбор жалобы «баланс минус у оплативших»** — оказалось не баг, а
   путаница в UI: долг на `/income/` и `/reports/debtors` рисуется со
   знаком «−» и красным (визуальный код «деньги ещё не пришли»), не
   реальное отрицательное сальдо. Переплаты ни у кого нет.
5. **Найдены, не решены — на подумать к следующей сессии:**
   - Коэффициент садик=школа×0.80 (решение 30-31.07) применён только при
     импорте, не на экране редактирования рецепта — два варианта решения
     на выбор владельца, см. [[architecture/menu_module]]
   - «Филе грудки птицы (мяготь)» у Махабат — нужно спросить у неё, какой
     из 3 предложенных вариантов имеет в виду, или это реально новый
     товар (прецедент — «Курица» намеренно не сведена к конкретной части)
6. **Июльские начисления удалены (закрыт открытый вопрос с прошлой
   сессии).** Абдусаттар подтвердил: родители в июле реально платили, но
   вне ИС (система в бою считается с 1 августа) — 8 июльских
   income-транзакций на 276.50 сом оказались тестовыми копеечными
   платежами с проверки Optima 14.07, к реальным оплатам родителей
   отношения не имеют. Удалены все 123 строки `Charge`
   (`description='Начисление за месяц'`, `date=2026-07-01`, org_id=4,
   сумма 842 258.10 сом) — before/after показан и подтверждён владельцем
   до удаления, прямым SQL (в системе нет UI-пути удаления начисления —
   ручная корректировка убрана целиком 04.07). Баланс детей теперь
   считается только с августа.

**Первым делом в следующей сессии:**
1. 🔴 Спросить владельца/Мунару/Айжан: озвучивали ли кому-то из родителей
   раздутые цифры долга 1–4 августа, пока баг был активен
2. Решить с владельцем — вариант А или Б для граммовки садик/школа на
   экране рецепта (см. выше и [[architecture/menu_module]])
3. Уточнить у Махабат по «Филе грудки птицы (мяготь)» — какой из трёх
   вариантов, или заводить новый товар
4. Сверить с Мунарой/Айжан статус июльского долга (закрыт вне системы
   или нет)
5. Продолжают числиться с прошлых сессий (см. ниже): фича «+ создать
   товар из рецепта» (план согласован, не реализована), 10 найденных
   дублей блюд, тестовые PIN 9001/9002, `txn_date` парсинг Optima,
   Соцфонд 12.25%, наполнение `/services/`, реестр сотрудников

---

**Сессия 30–31.07 (импорт реальной тех.карты на 4 недели — полная зачистка каталога блюд, дедуп, 3 реальных бага найдены и задеплоены) — 5 деплоев за сессию:**

1. **Каталог блюд полностью пересобран из реальных документов Махабат.**
   «Исправленный меню 2026-2027.docx» (эталонный список названий) + 4
   тех.карты по неделям (реальная граммовка садик/школа по каждому
   ингредиенту) — заменили тестовый импорт 27.07. Полная зачистка (не
   патч): удалены все 34 старых блюда + оставшиеся 16 записей меню
   (22–29.07), заведено 112 новых блюд с рецептом каждое (539 строк
   ингредиентов), 8 новых товаров склада. Проверено перед удалением: 0
   `write_offs` ссылалось на старые блюда — историю списаний не потеряли.
   Подробности методики (коэффициент 0.80, резолюция ингредиентов,
   штучная выпечка-исключение) — [[architecture/menu_module]].
2. **Дедуп каталога: два слоя проверки, не один.** Экран дублей (29.07)
   фильтрует по имени — на свежем импорте дал 58 кандидатов, из которых
   fuzzy-по-имени ложно предлагал слить РАЗНЫЕ каши (Пшеничная/Пшенная/
   Рисовая/Ячневая — общий шаблон фразы). Реальная проверка — по
   (товар, граммовка): только 6 из 58 оказались настоящими дублями, плюс
   4 отдельных, которых экран вообще не показывает (сходство имени ниже
   порога 60%). 52 ложных кандидата отклонены пакетно. **10 настоящих
   дублей — Абдусаттар сам сообщит Махабат**, не автослияние.
3. **Гейт «дубли перед меню» — сделан и в тот же день смягчён.** Первая
   версия жёстко блокировала `/menu/day`, пока очередь дублей не 0.
   Абдусаттар сам передумал: авто-списание считает каждое блюдо по
   своему `dish_id` независимо, дубль в каталоге не портит склад — только
   путает человека. Финал — мягкий баннер вместо блокировки, разовая
   чистка сегодня, не постоянный барьер.
4. **Найден и исправлен реальный баг**: `is_standard=False` прятал 18
   реальных товаров (Соль, Чай чёрный, Лук репчатый, Помидоры и др.) от
   поиска на экране рецепта блюда — пойман advisor'ом перед тем, как это
   стало бы плодить дубли товаров через будущую фичу «создать товар из
   рецепта». Фикс задеплоен, сама фича — не реализована, план согласован.
5. **Найден и исправлен второй баг**: строка рецепта без выбранного
   товара (`product_id = NULL`) раньше сохранялась молча и выпадала из
   авто-списания без единого сигнала — тот же класс «тихого нуля», что
   чинили 29.07 для чисел. Теперь блокирует сохранение всего рецепта.
6. **Открыто, не решено**: блюдо «Овсяное печенье» — тех.карта дала его
   как покупной снек (без рецепта), Абдусаттар переименовал в «домашнее»
   и заметил отсутствие муки — сейчас правит вручную через
   `/menu/dishes/{id}` как тест самого механизма ручной коррекции.
   Тестовый суп в `dish_ingredients` затронут не был (проверено вживую
   локально до и после смягчения гейта).

**Первым делом в следующей сессии:**
1. 🔴 Уточнить у Абдусаттара, разобрал ли он с Махабат 10 найденных
   дублей (6 на экране `/menu/dishes/duplicates`, 4 вручную через карточку
   блюда — список в wiki) — и решилось ли «Овсяное печенье»
   (покупное/домашнее, нужен ли реальный рецепт с мукой)
2. Реализовать фичу «+ создать товар прямо из рецепта» (план согласован
   с advisor'ом, фикс поиска уже задеплоен) — категория+единица
   обязательны, серверная проверка на похожий товар перед созданием
3. Два блюда сознательно оставлены нетронутыми при импорте (не входят в
   тех.карту 1:1) — «Запеканка из макарон с мясом курицы» (id63, курица
   вместо говядины по тех.карте) и «Рис отварной (гарнир)» (id78, без
   овощей) — уточнить у Махабат, актуальны ли ещё
4. Живая проверка с Мунарой/Махабат: понятен ли пересобранный каталог
   блюд, начала ли Махабат подбивать меню по дням на пустом (после
   зачистки) календаре
5. Продолжают числиться с прошлых сессий: `txn_date` парсинг (Optima),
   Соцфонд 12.25%, наполнение `/services/`, реестр сотрудников, сверка
   тарифов, деактивировать тестовый PIN 9001

---

**Сессия 29.07 (Optima — проверка боевого статуса; каталог блюд — редактирование рецепта, дедуп, «рецепт прежде чем в меню») — 4 деплоя за сессию:**

1. **Optima проверена на боевом проде напрямую через БД** (не по памяти) —
   50 активных детей Садика Сокулук, все с корректным PIN, `check` реально
   проходит с 17.07 (7 разных детей, растущая частота к 28.07), но **ни
   одного реального `pay`** ещё не было — только просмотр. Тестовый PIN
   9001 всё ещё `active` в проде (не критично, но стоит деактивировать
   перед стартом реальных оплат). Известный некритичный баг с парсингом
   `txn_date` (ISO8601 vs `YYYYMMDDHHMMSS`) по-прежнему не исправлен.
   Подробности — [[payments/optima]].
2. **Экран рецепта блюда реализован с нуля** — `/menu/dishes/` (список,
   статус рецепта ✓/⚠, поиск, «+ Новое блюдо»), `/menu/dishes/{id}`
   (просмотр/правка ингредиентов — автокомплит товара, грамм отдельно
   садик/школа, пустой рецепт сразу открывается в редактировании).
   Раньше `dish_ingredients` заводилась только разовым импортом из docx,
   ни одного экрана редактирования не было. Предупреждение «без рецепта»
   на `/warehouse/writeoff/auto` со ссылкой на нужный рецепт; после
   сохранения рецепта возвращает туда же (`return_date`) — иначе фикс
   терялся за идемпотентностью `auto_apply_if_pending` (поймано advisor'ом).
   Валидация на сохранении: строка без единого числа (садик/школа) или с
   нераспознанным значением («40г») больше не сохраняется молча.
3. **Найден и исправлен реальный баг**: фаззи-сравнение в `search_dishes()`
   было регистрозависимым («чй» против «Чай» — 40% вместо 80%) — часть
   причины, почему опечатки с другим регистром плодили дубли вместо
   совпадения. Починено для всех потребителей (автослияние блюд,
   автокомплит, экран похожих блюд).
4. **Дедуп каталога блюд** — `/menu/dishes/duplicates`: fuzzy-кандидаты на
   слияние (только предлагает, никогда не сливает молча — тот же урок,
   что с товарами склада), финальное решение всегда за человеком.
   Слияние переносит **все** FK на `dishes.id` (проверено запросом к
   information_schema, не угадано) — `MenuEntry` с дедупом по
   org/дата/приём пищи, `WriteOff` (репойнт, историю не теряем),
   `DishIngredient` (рецепт донора, если у survivor'а рецепта нет), новая
   служебная `DishMergeDismissed` (миграция `t9u0v1w2x3y4`). Первая версия
   трогала только `MenuEntry`/`DishIngredient` и падала 500-й на
   `WriteOff` — поймано advisor'ом до деплоя, не на проде.
   Переименование блюда (защита от переименования в уже занятое имя) и
   удаление — **одна кнопка**, без деления «безопасно/полностью»: удаляет
   блюдо всегда, каскадно чистит `MenuEntry`/`DishIngredient`/
   `DishMergeDismissed`, `WriteOff.dish_id` обнуляет (не удаляет — это
   факт склада, не карточка блюда). По прямому запросу Абдусаттара —
   Мунара должна иметь возможность чистить каталог без раздумий.
5. **Прод-очистка выполнена** (через сами роуты, не прямым SQL): 10
   мусорных блюд без единого использования и без рецепта удалены
   (борщ/компот/кекс/пончики/оромо и т.п., похоже — остатки с самого начала
   системы). Отдельно — 51 запись в Меню (20.07–30.07), ссылавшаяся на
   блюда без рецепта («чай» — 17 раз, «сухарики» — 6 раз, плюс 7 новых
   блюд на 30.07 и др.), удалена по прямому решению Абдусаттара («тестовый
   период, она заведёт заново») — карточки блюд остались в каталоге со
   статусом «рецепта нет», дни в меню опустели, ждут пересборки.
6. **Меню переведено на «рецепт прежде чем в меню» — архитектурный разворот.**
   `get_or_create_dish()` (свободный ввод + фаззи-автосоздание) больше не
   вызывается из `/menu/day` — добавить блюдо в Меню можно только выбором
   из автокомплита (`/menu/dishes/search?only_with_recipe=1`), и только
   если у блюда уже есть рецепт (проверка и на фронте, и на сервере —
   id из формы, клиенту не доверяем). Чипы «обычно даём» (`frequent_dishes`)
   убраны целиком — вместе с функцией в `dishes.py` (не осталось вызовов).
   Новое блюдо заводится только через `/dishes/new` — рецепт добавляется
   раньше, чем блюдо попадёт в меню, а не наоборот. **Реальный баг найден
   advisor'ом и воспроизведён до деплоя**: пересохранение дня (полный
   delete+insert, как и раньше) стирало уже стоящие в меню блюда без
   рецепта, если рядом добавлялось хоть одно блюдо с рецептом — пофикшено
   бронью `already_had` (блюда, которые на эту дату уже стояли в меню ДО
   сохранения, сохраняются независимо от рецепта; блокируются только
   НОВЫЕ попытки добавить нереципированное блюдо).
7. **Открытый вопрос, не решён**: стоит ли переименовать «Блюдо» во
   что-то нейтральнее — в каталоге есть однокомпонентные позиции вроде
   «Банан свежий»/«Порция сыра»/«Вареное яйцо», которые не готовятся, но
   технически корректно списывают со склада по норме на порцию. Решили
   не трогать до явного запроса.

**Первым делом в следующей сессии:**
1. 🔴 Живая проверка с Мунарой/Махабат: понятен ли новый порядок (рецепт
   → потом в меню) без объяснений, и пересобраны ли опустевшие дни меню
   (20.07–30.07) — 7 новых блюд на 30.07 и «чай»/«хлеб»/«сухарики» и т.п.
   ждут рецептов через `/menu/dishes/`
2. Деактивировать тестовый PIN 9001 на проде перед стартом реальных оплат
   родителей (см. п.1 выше)
3. Уточнить у Амана, почему пока нет ни одного реального `pay` при живых
   `check` — не тестовая ли активность банка ожидается дальше
4. Продолжают числиться с прошлых сессий: `txn_date` парсинг (Optima),
   Соцфонд 12.25%, наполнение `/services/`, реестр сотрудников, сверка
   тарифов

---

**Сессия 27.07 (авто-списание склада по тех.карте с нуля — от идеи до боевого запуска, найдено и исправлено 2 реальных бага) — построено за одну сессию:**

1. **Новый механизм: склад списывается сам по формуле меню × тех.карта × явка.**
   Это отменяет решение сессии 08–09.07 «V1 сознательно без нормы, рецептура
   складывается снизу вверх из фактических записей» — вместо этого impортирована
   реальная тех.карта школы (`docs/ТЕХ.КАРТА 2026.docx` + `docs/МЕНЮ БЕЗ ТЕХ
   КАРТ1.docx`, 89 блюд × ингредиенты, отдельно граммы садик/школа) как готовый
   источник нормы. Причина пивота: Абдусаттар прямо отверг «черновик +
   подтверждение» и штучное взвешивание поварами — у Махабат и поваров нет
   ресурса заходить на отдельный экран или взвешивать на кухне, значит
   списание должно проводиться **само**, без визита человека на специальный
   экран. Технически — тот же приём «ленивого триггера», что и в
   `generate_monthly_charges` (billing.py): вызывается при заходе на уже
   посещаемые страницы (Склад, Расходы), не отдельным кроном. Полная новая
   архитектура (модели, роуты, идемпотентность, ограничения) —
   [[architecture/warehouse_module]].
2. **Найден и исправлен баг двойного списания**: 5 блюд, встречавшихся в
   тех.карте в нескольких недельных таблицах, получили при импорте **задвоенный
   (местами утроенный) набор ингредиентов** — расчёт списания завышал их расход
   в 1.5–2 раза. Проверено: уже проведённое боевое списание 24.07 этих блюд не
   касалось, откатывать не пришлось. 12 лишних строк удалены вручную по
   утверждённому с Абдусаттаром правилу «более новая версия рецепта побеждает».
3. **Реконсиляция 151 → 0 непривязанных ингредиентов тех.карты.** Fuzzy-подбор
   по существующему каталогу (`rank_candidates`, тот же алгоритм, что и для
   чеков) оказался ненадёжен даже на эталонных товарах — тот же класс ложных
   срабатываний, что чинили в меню (Сахар-песок↔Чеснок, Курица↔Корица и т.д.).
   Сверка сделана вручную построчно с Абдусаттаром: 11 названий привязаны к уже
   существующим товарам (Крупа рисовая→Рис и т.п.), 35 новых товаров заведены
   (остаток 0, категория проставлена), 1 позиция («Огурцы или томаты свежие» —
   реальная формулировка «или-или» из документа для одного салата) осознанно
   оставлена непривязанной. Полный список — в wiki.
4. **«Курица» из тех.карты — намеренно НЕ привязана** ни к одной из конкретных
   куриных частей в каталоге (Тушка/Филе/Бедро и т.д.) — заведена отдельным
   обобщённым товаром, потому что по факту закупки часть варьируется (то целая
   тушка, то нарезка), а тех.карта не уточняет, какая.
5. **«Томатная паста» переведена с единицы «уп» на «кг`»** — обнаружен
   отдельный, не сегодняшний баг: механизм «запомни вес» (кнопка на экране
   авто-списания) реально работает только для unit="шт", для любой другой
   нестандартной единицы («уп» и т.п.) он молча ничего не даёт. Для Томатной
   пасты решили проще — сменить единицу, не трогая код. Других товаров с этой
   проблемой пока не найдено, но подход стоит помнить при заведении новых
   товаров в упаковках.
6. **Баннер «на завтра не хватает» на Складе** показывает только
   *непривязанное* сырьё, а не реальный дефицит остатка — сознательно (иначе
   34 позиции вроде Сахар-песка светились бы каждый день до первой
   инвентаризации). Прогнан через реальный браузер на проде, отрисовывается
   корректно.

**Первым делом в следующей сессии:**
1. 🔴 Живая проверка с Махабат/поварами: реальный рабочий день на новом
   механизме авто-списания — видят ли баннер, не удивляет ли их исчезновение
   ручного экрана списания по меню, нормально ли выглядят цифры на их блюдах
2. Вес 1 шт «Хлеб» так и не введён Махабат — механизм ждёт (не блокирует,
   просто просит вписывать вручную каждый раз, пока не введут через
   «запомнить» на экране авто-списания)
3. Актуализация склада для 35 новых товаров с остатком 0 — как только Махабат
   физически посчитает, что реально есть на складе
4. Из сессии 23.07 (ещё не сделано, не относится к сегодняшнему): проверить,
   что дата Айбике Анарбековой поправлена на 01.06.2026 через
   `/students/bulk-dates`; живая проверка Махабат трёх фиксов Меню и колонки
   «Оплата (месяц)»

---

**Сессия 23.07 (три реальных бага в Меню найдены и задеплоены + колонка «Оплата (месяц)» в Детях) — 2 деплоя за сессию:**

1. **Fuzzy-match блюд в меню ложно сливал разные блюда — защита от опечаток
   работала только в одну сторону.** Реальный кейс: «Рыбьи биточки на пару»
   молча подменялось на «. Овсяная каша на молоке со сливочным маслом»
   (WRatio 85.5 из-за сильного расхождения длин строк). Первый фикс проверял
   длину только одним направлением, пропустил зеркальный случай — финальная
   защита симметричная. Подробности и regression-проверка —
   [[architecture/menu_module]].
2. **Гонка автокомплита** — устаревшая подсказка могла всплыть уже после
   Enter и случайный клик рядом добавлял блюдо повторно. Фикс: `render()`
   сверяет запрос с текущим значением поля.
3. **Гонка сохранения дня — настоящая причина большинства дублей на проде**
   (не автокомплит). Параллельные `POST /menu/day` для одной даты (быстрый
   ввод нескольких блюд подряд) ничем не сериализовались — задваивали весь
   день целиком. Фикс: `pg_advisory_xact_lock(org_id, дата)`. Воспроизведено
   и проверено детерминированно (искусственная задержка + 5 параллельных
   запросов, до и после фикса).
4. **Испорченные данные на проде почищены** (дубли 20/23/27.07) через тот же
   `/menu/day` эндпоинт, что и форма — не прямой SQL. «Рыбьи биточки на
   пару» не восстановлены (никогда не сохранялись под своим именем),
   Махабат ввела заново после фикса.
5. **Колонка «Оплата (месяц)» в `/students/`** — фактическое начисление за
   текущий месяц на каждого ребёнка (легаси-тариф, скидка, заморозка%,
   допуслуги — не пересчёт, а существующий `Charge`). Подробности —
   [[architecture/billing_module]].
6. **Дата поступления Айбике Анарбековой уточнена, но НЕ применена в этой
   сессии** — реальная дата 01.06.2026 (только год был битым — `0006`),
   подтверждено Абдусаттаром. Важный нюанс: эта дата **ровно на границе**
   переходного тарифа (`legacy_tariff_cutoff=01.06.2026`) — по решению
   Абдусаттара такой ребёнок должен платить **новую** цену (10000), что и
   происходит автоматически при строгом `<` в `billing.py:70`, менять код
   не нужно. Абдусаттар должен был поправить дату сам через UI (клик именно
   в сегмент ГОДА поля даты на `/students/bulk-dates`, day/month уже верные)
   — **не подтверждено, что сделано**, проверить в начале следующей сессии.

**Первым делом в следующей сессии:**
1. 🔴 Проверить, что дата Айбике Анарбековой реально поправлена на
   01.06.2026 (через `/students/bulk-dates`, сегмент года) — и что тариф
   после этого корректно встал на 10000, не 8000
2. Живая проверка колонки «Оплата (месяц)» и трёх фиксов меню на реальном
   использовании Махабат (не только Playwright/live-запросы этой сессии)
3. Продолжают числиться с прошлых сессий: дождаться 01.09.2026 (истечение
   переходного периода), сверка тарифов, Соцфонд 12.25%, наполнение
   `/services/`, реестр сотрудников

---

**Сессия 22.07 (переходный тариф переделан на динамический расчёт + модуль «Дети» — full UX-аудит и фиксы) — 6 деплоев за сессию:**

1. **Переходный тариф («старая цена» для детей, зачисленных до повышения) переделан с разового
   снимка на полностью динамический расчёт.** Раньше кнопка «Зафиксировать» проставляла
   `Student.legacy_tariff_amount` один раз всем подходящим на тот момент детям — если после
   этого поправить дату поступления, снимок не обновлялся без повторного клика (реальный
   инцидент 22.07: цену подняли раньше, чем успели зафиксировать старых). Теперь
   `Organization.legacy_tariff_cutoff/price/until` — постоянная настройка объекта, а
   `billing._tuition_fee()` каждый раз при начислении сам вычисляет цену по факту
   **непрерывности зачисления** (`billing.continuous_enrollment_since` — идёт по `Enrollment`
   назад до первого настоящего разрыва, отличает «непрерывно с 2026» от «был старым, выбыл,
   вернулся после границы» — второй случай НЕ грандфазерится, платит новую цену сразу).
   Обсуждено через advisor (отвергнут наивный подход через `discount_amount` — 44 ребёнка уже
   имеют реальные скидки по другим причинам). Подробности — [[architecture/billing_module]].
2. **Найден и исправлен реальный баг**: карточка ребёнка (`/students/{id}/edit`) считала
   тариф отдельным дублирующим кодом прямо из `Service.price`, вообще не зная о переходном
   тарифе — показывала 10000 вместо реальных 8000 «старым» детям, хотя настоящее начисление
   в конце месяца уже считало верно. Вынесено в `billing.tuition_base_price()`, переиспользуется
   и в карточке, и в лимите скидки при сохранении.
3. **Бейдж тарифа добавлен в `/students/bulk-dates`** — Махабат сразу видит, сколько будет
   платить каждый ребёнок (по той же логике непрерывности, без лишнего запроса на ребёнка —
   `billing.continuous_since_from_enrollments` поверх уже загруженных `Enrollment`), с пометкой
   «(до 01.09)» у временной цены, чтобы не путали с постоянной скидкой.
4. **На проде настроено для Садика Сокулук**: граница 01.06.2026, цена для старых 8000,
   действует до 01.09.2026 — 2 реальных ребёнка сейчас на старой цене (Айбийке Анарбекова,
   Русабекова Амина). У Айбийке в базе битая дата зачисления (`0006-06-01` вместо реального
   года) — на результат не влияет (любая древняя дата < границы), но стоит поправить на
   правильную, когда будет удобно уточнить у Махабат/по факту.
5. **Модуль «Дети» — полный UX-аудит через frontend-design** (по явному запросу — «весь блок,
   не узкую правку»): sticky-кнопка сохранения на bulk-dates И на посещаемости, клавиатурная
   доступность списка детей, починен баг сброса фильтра, добавлен фильтр по месяцу в истории
   платежей ребёнка. Плюс раньше в этой же сессии: поле «Группа» на `/students/add`, модалка
   для отметки отсутствия периодом вместо формы наверху страницы, N+1 фикс в bulk-dates-save
   (раньше пересохраняла всех ~90 детей объекта на любую правку одного).
6. **История цены услуги** (`ServicePriceHistory`) + IA-консолидация `/services/` — «Скидка при
   заморожен» и «Переходный период» перенесены внутрь карточки «Обучение» (были обособлены
   наверху, не читались как относящиеся к тарифу).

**Первым делом в следующей сессии:**
1. Уточнить у Махабат реальную дату поступления Айбийке Анарбековой (сейчас `0006-06-01` —
   технически не мешает расчёту, но стоит исправить)
2. Дождаться 01.09.2026 — переходный период истекает, оба «старых» ребёнка автоматически
   переходят на 10000 без какого-либо действия (проверить один раз, что так и произошло)
3. Продолжают числиться с прошлых сессий (см. ниже): сверка тарифов, Соцфонд 12.25%,
   наполнение `/services/` реальными доп.услугами, реестр сотрудников

---

**Сессия 16.07, продолжение 3 (лента платежей поставщикам, PIN-фикс, статус «Заморожен», задачи Махабат) — 8 деплоев за сессию:**

1. **Лента платежей поставщикам** (`/suppliers/{id}`) — баланс на лету (закупы +
   начальное сальдо − платежи, FIFO), не мутирует `Transaction.amount_paid`. История
   группируется по чеку (один визит = одна строка), не по отдельной проводке.
   Подробности — [[architecture/expenses_module]].
2. **Фикс PIN**: тестовые фикстуры банка 9001/9002 больше не сдвигают счётчик новых
   детей (был бы 9003 вместо 0097). [[architecture/billing_module]].
3. **Пропорциональное начисление** — если `Enrollment.start_date` попадает в текущий
   расчётный месяц, начисляется не полная сумма, а по дням. Безопасно для уже
   начисленного июля (идемпотентность), с августа мигрционная дата (03.07, у 78/79
   детей) перестаёт на что-либо влиять.
4. **`/students/bulk-dates`** — одноразовый бэкфилл статуса/группы/даты поступления
   разом по всем детям объекта, вместо похода в каждую карточку. Заведена
   «Подготовительная группа» для Садика Сокулук.
5. **Статус ребёнка «Заморожен»** — место в группе держится (Enrollment не
   закрывается), начисляется `Organization.frozen_discount_percent` (настройка
   объекта, дефолт 50%, сейчас реально нужен только Садику Сокулук — подтверждено
   Махабат). Optima пропускает оплату. Месяц самой заморозки не пересчитывается
   (принято осознанно — см. wiki, дешевле устного решения с родителем, чем
   возвращать убранную 04.07 ручную корректировку начислений).
6. Карточка нового ребёнка теперь сразу открывается в редактируемом виде — не нужно
   лишний раз жать «Изменить», чтобы увидеть поле «Группа».
7. **Роль Махабат расширилась** (с 15.07 — главный учётчик Садика Сокулук): закупы,
   поставщики, долги поставщикам, актуализация данных детей, меню. Текст для
   Telegram-группы подготовлен и согласован с Абдусаттаром, **но не отправлен** — у
   Claude Code нет доступа к боту группы в этой сессии (`TELEGRAM_TOKEN` в
   `.env.example` — placeholder, нигде в коде не используется). Текст нужно
   скопировать и отправить вручную.

**Первым делом в следующей сессии:**
1. Уточнить у Абдусаттара, отправлено ли сообщение Махабат и как прошло знакомство с
   задачами
2. Статус «Заморожен»/«Бронь» для Школы/Кожомкул — НЕ реализовывать заранее, только
   если появится конкретный запрос от Айжан/Мунары (сейчас гипотетично, осознанно не
   спроектировано)

---

**Сессия 16.07, продолжение (чипы-подсказки в Меню — перепроверено и задеплоено; режим просмотра/редактирования реализован):**

1. **Фикс чипов из 15.07 подтверждён вживую** (Playwright, локально) и
   задеплоен на прод. Клик по чипу «Булочка» корректно добавил блюдо
   в поле выбранных (синяя подсветка чипа + новая плашка «Булочка ×»
   ниже), ошибок в консоли браузера нет.
2. **Режим просмотра/редактирования по дням в `/menu/` реализован и
   задеплоен** — план из записи 15.07 ниже выполнен полностью:
   `has_entries` в `app/routers/menu.py`, `toggleDay()` в
   `menu/form.html`. Проверено вживую (Playwright) — переключение в
   edit сохраняет уже выбранное блюдо, «Сохранить меню» остаётся общей
   кнопкой на весь диапазон. Подробности — [[architecture/menu_module]].

**Сессия 15.07 (баг с чипами-подсказками в Меню + обсуждён режим просмотра/редактирования):**

1. **Найден и исправлен реальный баг**: серые кнопки-подсказки «обычно
   даём» (`chip-pick`) в `/menu/` не реагировали на клик вообще ни для
   одного блюда. Причина — `onclick="addChip(...,{{ d.name|tojson }})"`:
   `tojson` всегда оборачивает строку в двойные кавычки, а сам атрибут
   `onclick="..."` тоже в двойных — кавычки от `tojson` обрывали атрибут
   раньше времени, ломая HTML/JS независимо от конкретного названия блюда
   (системный баг, не зависел от данных). Исправлено в
   `app/templates/menu/form.html`: убран inline `onclick`, кнопки теперь
   несут `data-cell/data-date/data-meal/data-name` (экранируются Jinja
   штатно), клик навешен через `addEventListener`. Проверено рендером
   шаблона напрямую через Jinja2 (Docker недоступен в этой машине — не
   было полноценной живой проверки в браузере, только статическая
   проверка корректности HTML/атрибутов, включая кавычку в названии
   блюда). **Стоит перепроверить вживую в браузере при первой
   возможности.**
2. **Обсуждён и согласован (но не реализован) режим просмотра/
   редактирования для `/menu/` по дням** — по аналогии с уже принятым
   паттерном у Расходов (просмотр→«Изменить»→правка, применяется с 04.07
   на карточке ребёнка и с 12-13.07 на чеках/посещаемости). План: день с
   уже сохранённым меню открывается сначала read-only (список блюд по
   приёмам пищи текстом) с кнопкой «✎ Редактировать»; пустой день —
   сразу в текущем редактируемом виде без доп. клика. Общая кнопка
   «Сохранить меню» остаётся одна на всю неделю — данные просмотровых
   дней едут скрытыми полями, не теряются. Правки только в
   `app/templates/menu/form.html` (+ пометка `has_entries` в
   `app/routers/menu.py`), бэкенд сохранения не трогается. **Не начато
   писать код** — сессия прервалась на разборе бага с кнопками.

---

**Сессия 14.07 (тестирование Optima банком + 3 реальных бага, все задеплоены):**

1. **Тестирование check+pay с банком продолжено** — прогнаны PIN 9001 (активный)
   и 9002 (переведён в `inactive` по просьбе банка), сценарий «ребёнок выбыл»
   (код 79) подтверждён рабочим, платёж по неактивному PIN не создаёт
   `Transaction`. Платежи с копейками (35.50 сом) дошли и записались точно.
2. **Найден и исправлен баг доступа**: `PRICE_EDITORS`/`PILOT_USER_IDS`
   (заведены 12-13.07) ссылались на несуществующие id `61/64` вместо реальных
   id Айдай/Талас (`5/6`) — из-за этого они не могли менять цену услуги, а
   пилот-ограничение (только «Садик Сокулук») на них фактически не действовало,
   видели все объекты. Исправлено и задеплоено (`90c07e6`).
3. **Добавлен лог всех попыток Optima** (`optima_log` — новая таблица,
   `/optima/log`) — раньше неуспешные попытки (неверный PIN, ребёнок выбыл,
   дубликат) были видны только через `railway logs` в CLI. Страница видна
   **только Абдусаттару** (`user_id == 1`, хардкод, не по роли — техническая
   диагностика, не бизнес-функция пилота). Каждый check/pay пишется туда
   независимо от результата.
4. **Банк сообщил о 2 проблемах, обе устранены**:
   - «Копейки не отрабатываются» — на деле в БД копейки хранились и считались
     точно всегда; проблема была чисто в отображении (`{:,.0f}` округлял до
     целых сом в UI). Поправлено на `{:,.2f}` в «Оплатах», истории ребёнка,
     «Долги/переплаты» (зарплаты/расходы не трогали — там копеек не бывает).
   - «Время указано некорректно» — сервер и БД работали в UTC, а не в
     Бишкекском времени (расхождение ровно 6 часов). Поставлена таймзона
     `Asia/Bishkek` — в Dockerfile (tzdata + `ENV TZ`) и в самой БД
     (`ALTER DATABASE ... SET timezone`, прод и локально). Проверено сквозным
     тестом: новый лог-таймстемп на проде показал верное бишкекское время.
   Подробности — [[payments/optima]].

**Незакрытый мелкий хвост (не исправлялся сознательно, не спрашивали):**
`txn_date` от банка приходит в ISO 8601 (`2026-02-19T10:50:39.882Z`), наш
парсер (`app/routers/optima.py`) ожидает `YYYYMMDDHHMMSS` без разделителей —
парсинг молча падает и подставляется `date.today()` вместо даты банка. После
фикса таймзоны риск невелик (только полночная граница), но если банк начнёт
слать даты прошлых чисел (запоздалая обработка) — дата платежа будет неверной.
Стоит поправить парсинг, когда будет минутка.

**Сессия 13.07, вторая часть (UX-доводка Расходов + категории расходов по плану счетов КР + два реальных бага):**

1. **Переименование «Закуп»→«Расходы»** везде в UI (заголовок вкладки, `<h1>`,
   ссылка «← Закуп»), кнопки шапки унифицированы (убраны эмодзи, один primary).
2. **`/expenses/recurring` — полная доводка usability**: автоприменение фильтра
   месяца (заодно исправлен реальный pre-existing 500-баг —
   `date.fromisoformat` не понимал формат `YYYY-MM`, который отдаёт браузер),
   убрана «+ Ещё одна» у ФОТ (теперь одна проводка в месяц как у всех
   остальных), подпись «Дата оплаты», «Оклады сотрудников →» вместо вводящей
   в заблуждение «Исправить оклад», статус «Проведено» — зелёный бейдж с
   полноразмерными кнопками вместо мелкого текста, сумма ФОТ теперь
   нередактируема (read-only, вычисляется из окладов сотрудников).
   Подробности — [[architecture/expenses_module]].
3. **Категории расходов приведены к типовому плану счетов МФ КР** — по
   инициативе Абдусаттара («не гадать самим, свериться с фин.грамотностью»).
   Консультация через уже имеющийся ресурс финэксперта
   (`nb1_cfo_operations/plan_schetov_1c_mapping.md` + NotebookLM). Две миграции:
   - `n4o5p6q7r8s9`: «Ежемесячные расходы» → **ФОТ / Коммунальные расходы /
     Связь / Операционные услуги**. Охрана намеренно НЕ в «Административные»
     (7310) — по совету эксперта это 7160 (обслуживание объекта), не
     консалтинг. Шаблон «Газ» добавлен, «Отопление» удалено (0 проводок).
   - `o5p6q7r8s9t0`: Питание+Хозяйство+Текущий ремонт сгруппированы под новый
     родитель **«Товары»** (плоская структура — рендер категорий по коду
     нигде не поддерживает 3 уровня вложенности). **«Капитальный ремонт»
     вынесен отдельным корнем** — CAPEX, не смешивается с товарными
     OPEX-категориями. Переименования: Готовая еда→Услуги питания, Питьевая
     вода→Бутилированная вода, Мыломойка→Хозяйственные материалы, Канцтовары→
     Канцелярские товары, Текущий ремонт→Текущий ремонт имущества.
     **«Игрушки» НЕ переименованы в «МБП»** — разные по природе вещи (название
     товара vs широкая учётная классификация), решено не путать персонал.
   - **Найден реальный пробел (в бэклог, не исправлен)**: отчисления в
     Соцфонд (12.25% сверх зарплаты) нигде не учитываются — занижает
     себестоимость персонала в будущих отчётах примерно на 12%.
   - Отложено до фактов от Мунары/Айжан: Аренда (зависит от того, в
     собственности ли здание), Административные расходы, Медикаменты/Мягкий
     инвентарь/ГСМ.
   Подробности — [[architecture/expenses_module]].
4. **Два реальных бага найдены и исправлены** (существовали независимо от
   сегодняшних правок, всплыли при живом тестировании):
   - Кнопка удаления в списке `/expenses/` была нерабочей для всех строк без
     квитанции (Сервис и Ежемесячные) — слала `POST /expenses/None/delete`.
   - `delete_receipt()` падал нарушением внешнего ключа при удалении ЛЮБОЙ
     квитанции с хотя бы одной проведённой транзакцией — по факту кнопка не
     работала ни разу на реально подтверждённом чеке.
5. Клик по Ежемесячной проводке в общем списке теперь ведёт на
   `/expenses/recurring?month=...` (не в общую форму редактирования), кнопка
   удаления в списке для неё скрыта (своя есть на самой странице).
6. **Колонка «Объект» убрана** из списка расходов (пилот сейчас на одном
   объекте одновременно на пользователя) — заменена подписью-комментарием
   под категорией (для Ежемесячных = имя шаблона, для Сервис = обязательный
   комментарий). **Вернуть колонку**, когда Школа/Кожомкул подключатся и
   один пользователь увидит несколько объектов сразу.
7. `edit_tx.html` (Сервис/ручные расходы) переведён на паттерн
   просмотр→«Изменить»→правка (тот же, что у карточки ребёнка с 04.07) —
   клик по строке больше не открывает сразу редактируемую форму. Удаление
   переехало внутрь режима редактирования.
8. `confirm.html`: у уже подтверждённых квитанций («Проведено!») раньше не
   было вообще никакого действия — добавлена «Удалить квитанцию».
   Полноценное редактирование уже подтверждённых квитанций — сознательно
   отложено, отдельная более крупная задача.
9. Очищено 6 старых тестовых записей из локальной БД (долг-тесты с 06.07 +
   e2e-тест с квитанцией и фото).
10. **Посещаемость разобрана, не реализовывалась.** Подтверждено: полный
    журнал по дням формируется в БД (present/absent на каждого активного
    ребёнка, каждый сохранённый день — не только по отмеченным
    отсутствующим), но таблица `Attendance` нигде не используется за
    пределами `attendance.py` — задумка 09.07 про Меню/факт-готовку так и
    не подключена. Журнал за несколько дней сразу (календарь-обзор) решили
    пока не нужен — точечная навигация по датам (уже есть) достаточна.
    **Идея на будущее** (месячный вид на карточке ребёнка с подсветкой
    пропусков, обязательно связанный с фактическим уменьшением `Charge`, не
    просто отчёт) записана в `context/ideas.md` (13.07).
11. **`/services/` подтверждено пуст** (0 записей) — старый незакрытый
    пункт с 04.07. Основная услуга садика (посещение) намеренно не в этом
    справочнике — это `Student.extra.monthly_fee`.

12. **✅ Деплой на прод выполнен** (13+1 коммитов, 12 миграций — весь пакет
    с сессии 10.07 по сегодня: Меню/Сотрудники, Ежемесячные+скидка,
    консолидации, `period`, PILOT_USER_IDS, весь сегодняшний UX-пакет и
    категории по плану счетов КР). Прод сейчас на коммите `5170bac`.
    **По пути упал один раз** — миграция «Товары» не учла
    `products.expense_category_id`, указывающий прямо на старые корневые
    категории (2 товара на проде, заведены до появления подкатегорий) —
    DDL упал на FK, контейнер не поднялся (502 на несколько минут).
    Спасло то, что весь `alembic upgrade head` идёт в одной транзакции
    (`env.py`) — откат был полным, БД не пострадала. Пофиксено и
    передеплоено успешно, подробности — [[architecture/deploy_ops]].
    Regression прогнан вживую на реальном проде (11 ключевых страниц,
    все 200, ноль ошибок консоли).
13. **«Ученики» → «Дети»** везде в UI. Список по умолчанию сгруппирован по
    группе (одна таблица, tbody-секции — не отдельные таблицы, иначе
    ширина колонок расходилась между группами), дети без группы — секцией
    в конце. Подробности в коде, не в wiki (мелкая правка).
14. **Посещаемость перенесена в «Дети»** — убрана из бокового меню как
    отдельный пункт, доступ кнопкой со страницы `/students/`. «Дети»
    подсвечивается активным и на странице Посещаемости, добавлена ссылка
    «← Дети». Переведена на паттерн просмотр→«Изменить»→правка: если на
    дату уже сохранены отметки — показывается справка «Отсутствуют: ...» /
    «Все пришли» по группам, чекбоксы скрыты до явного «Изменить».
15. **✅ Тариф за учёбу переделан на реальную услугу + скидка в сомах**
    (оказалось, не терпело до следующей сессии — сделано в этот же вечер).
    `Service.is_tuition=True` — базовый тариф (8000, редактируется на
    `/services/`, не константа в коде — тариф будет расти), применяется
    автоматически всем активным детям объекта, удаление на сервере
    заблокировано. `Student.discount_amount` (заменил неиспользуемый
    `discount_percent`) — скидка в сомах персонально ребёнку; форма карточки
    дополнена полями «Скидка»/«Причина» (их не было вообще, хотя backend-
    валидация существовала с 12.07). Миграции `p6q7r8s9t0u1`+`q7r8s9t0u1v2`
    перенесли старые `monthly_fee` в `discount_amount` — проверено на
    реальных 96 детях, распределение начислений после пересчёта совпало
    с исходным один в один (никого не задело задним числом), 12 детей без
    тарифа теперь корректно получают базовые 8000 (раньше не начислялось
    вообще). Подробности — [[architecture/billing_module]].
16. **Пункты 13-15 НЕ задеплоены** — только локально, после большого
    деплоя (п.12) сознательно взята пауза, не гнать ещё один пуш в конце
    той же ночной сессии.

**Первым делом в следующей сессии:**
1. 🔴 Задеплоить пункты 13-15 (Дети/Посещаемость/тариф-услуга) — включает
   2 новые миграции данных, прогнать на проде так же аккуратно, как
   локально (проверить реальное распределение начислений после пересчёта)
2. Соцфонд 12.25% — решить, встраивать ли в расчёт ФОТ отдельной строкой
   или как множитель
3. Дождаться ответа Мунары/Айжан: Сокулук в аренде или собственности,
   какие ещё административные расходы реальны, и сверка самих тарифов
   (`monthly_fee`) — тянется много сессий, теперь есть зацепка (см. п.15)
5. Наполнить `/services/` реальными доп.услугами (тянется с 04.07)
6. Реестр сотрудников почти пуст (2 человека)
5. Живая проверка на реальном использовании персоналом — понятны ли новые
   категории/экраны без объяснений (как и раньше планировалось для
   предыдущего пакета, ещё не проверено на практике)

---

**Сессия 12.07 (грумминг каталога продуктов + пользователи-тестировщики + разбор процессных вопросов):**

Локалка не поднимается через Docker (не установлен на машине) — вместо
`docker-compose` используется нативный Postgres-сервис Windows
(`postgresql-x64-16`, порт 5432, БД `pearl`) + прямой `uvicorn app.main:app
--port 8001`. Обновлено в разделе «Локальная разработка» ниже.

1. **Пользователи-тестировщики перераспределены**: все, кроме Айжан (осталась
   на org_id=2 «Школа», она реальный директор школы), переведены на org_id=4
   «Садик Сокулук» — Абдусаттар, Мунара, Айдай, Махабат. Причина: тестируем
   сначала на одном объекте по приоритету, Айжан подключается вторым этапом.
   **Заведён новый пользователь Талас** (role=`owner`, org_id=4, пароль `005`)
   — тоже собственник, добавлен в `scripts/add_user.py` рядом с Айдай.
   Применено и на локалке, и на проде (Айдай тоже создана на проде — раньше
   её там не было, скрипт не запускали).
2. **Грумминг каталога продуктов — локалка и прод.** На локалке было 53
   товара без категории: 17 — синтетический тестовый мусор (стресс-тест OCR,
   одни и те же бессмысленные слова с рандомными ценами по чекам #36/40/44,
   плюс явный «Тестовый товар А» на #37 — удалены целиком, включая чеки и
   транзакции), 1 — «за дорогу» 2000 сом (транспортный расход, ошибочно
   вписанный как товар в фото-квитанцию — удалён из позиций, это не товар),
   35 — реальные позиции с настоящих сканов, обобщены/докатегоризированы или
   слиты алиасами в существующие эталоны (Кран, Труба, Муфта, Крепёж, Колено,
   Огурцы, Масло сливочное, Порошок стиральный / Средство для мытья посуды —
   разведены как два разных товара под одним старым именем, и т.д.). Осталось
   9 нечитаемых (OCR/почерк не разобрать даже по фото) — оставлены как есть,
   разбирать по фото при возможности. На проде было 7 без категории (все
   реальные, заведены самим Абдусаттаром на чеке №4 от 06.07, 0 привязанных
   транзакций) — обобщены и докатегоризированы полностью, «Без категории» = 0.
   **Метод**: не гадать по одному имени товара, а поднимать `receipt_items` +
   исходный файл чека — резко снижает риск неверного обобщения и отличает
   синтетику от реальных позиций (синтетика узнаётся по повторяющимся
   бессмысленным словам с разными случайными ценами на разных чеках).
3. **Найден и задокументирован реальный механизм бага «два имени — один
   товар»**: при `POST /confirm` итоговое совпадение ищется по точному
   совпадению текста в поле «Название» на экране (`get_or_create_product`),
   а не по ИИ-подсказке (`normalize_items`). Если человек печатает с нуля
   вместо выбора из автокомплита — можно случайно попасть точным совпадением
   в чужой уже существующий товар (так «фейри» стало «порошком»). Проверено
   эмпирически: сам ИИ-матчер сейчас работает верно (сводит «фейри»/«ферри» к
   одному товару) — проблема не в нём, а в обходе автокомплита. Существующее
   правило («жёлтая рамка — смотри подсказки, не печатай с нуля») уже
   покрывает это, просто не было соблюдено при тестировании.
4. **Подтверждено (важно для ожиданий): категоризация не задним числом.**
   `Transaction.category_id` выставляется один раз при подтверждении чека и
   никогда не пересчитывается автоматически, если потом поменять
   `product.expense_category_id`. Значит грумминг каталога чинит будущее
   сопоставление, но не переправляет уже проведённые суммы — для этого нужно
   вручную поправить и товар, и конкретную транзакцию. Счётчик «Без
   категории: N» на `/expenses/` уже считает именно транзакции (не карточки
   товаров) — правильная метрика для отслеживания, ничего строить не нужно.
5. **Сервисные расходы (такси, банк.комиссии, курьер) уже полностью
   поддержаны, без кода.** `/expenses/add` (ручной ввод) уже позволяет
   провести расход одной суммой на выбранную категорию без товарных позиций
   — специально для Транспорт/Прочее форма даже не показывает блок позиций.
   Инцидент с «за дорогу» — это чек вписали не в ту форму (фото-квитанция с
   обязательной итемизацией вместо `/expenses/add`), не пробел в функционале.
   Категория «Банковские комиссии» заведена (миграция `f6g7h8i9j0k1`,
   root-leaf, добавлена в `SERVICE_CAT_NAMES` в `expenses.py:859`) — **пока
   только на локалке**, не задеплоена и не закоммичена, ждёт общего пуша
   вместе с меню/сотрудниками.
6. **Открытый дизайн-вопрос: повторяющиеся ежемесячные расходы** (зарплата,
   коммуналка, охрана) — НЕ то же самое, что решение 10.07 про ФОТ
   (живая сумма, не Transaction). Новая идея — с ручным триггером/подтверждением
   (Мунара проверяет/правит, потом сама запускает) — снимает возражения
   10.07 (пустой список, смена оклада посреди месяца), но требует отдельного
   проектирования. Два разных подслучая, не одна фича:
   - **Зарплата/Охрана** — сумма стабильна, шаблон с последним известным
     значением, человек подтверждает или правит
   - **Коммуналка** — сумма каждый месяц своя (по счётчику) — нужен не
     автоповтор суммы, а просто напоминание-чеклист без предзаполнения
   Не спроектировано и не реализовано — зафиксировать в
   [[blueprints/unit_economics_module]] как открытый вопрос следующей сессии.
7. **Скидка — подтверждено, что не существует нигде** (`grep discount|скидка`
   → 0 совпадений в `app/`). Садик-услуга не в `Service` намеренно (это
   `Student.extra.monthly_fee`, индивидуальный тариф, не общая цена на
   объект) — не баг. Единственная обсуждавшаяся скидка (минус 100 сом/день
   пропуска, решение Мунары 10.07) — по-прежнему не реализована, старый
   открытый пункт, см. [[architecture/billing_module]].
8. **Мелкий долг на будущее**: деление категорий «товар/услуга» в коде
   зашито как список имён (`SERVICE_CAT_NAMES = {"транспорт", "прочее"}` в
   `expenses.py:859`), не как поле на `ExpenseCategory`. Новые сервисные
   категории (Банк.комиссии, курьер) в этот список не попадут автоматически.
   Если понадобится ещё где-то различать товар/услугу (например, визуальное
   разделение «Закупа» на странице) — заводить нормальный булев флаг на
   категории, не плодить список в коде (уже есть один такой мёртвый флаг,
   `warehouse_eligible`, — не повторять).
9. **«Закуп» = общий реестр всех расходов**, не только товарных — название
   страницы вводит в заблуждение, но фильтр по категории уже есть
   (`list.html:25`), им можно уже сегодня отделить сервисные расходы от
   товарных при просмотре.

---

**Сессия 10.07 (blueprint-процесс + Меню + Сотрудники/ФОТ/Амортизация):**

Введён новый процесс: перед реализацией модуля — blueprint в
`wiki/blueprints/`, после реализации переносится в `wiki/architecture/`.
Применён дважды за сессию.

1. **Меню/приёмы пищи реализовано** — `Dish`/`MenuEntry`, `/menu/`
   (карточки по дням, чипы «обычно даём», защита от опечаток),
   `WriteOff.dish_id` на списании с мягким предупреждением. Подробности —
   [[architecture/menu_module]].
2. **Сотрудники/ФОТ/Амортизация реализовано** — `Employee`, `/employees/`
   (только owner/founder), `Asset.useful_life_months`. **Архитектурное
   решение**: ФОТ и Амортизация считаются на лету (чистые функции), НЕ
   хранятся как `Transaction` — паттерн начислений (`generate_monthly_charges`)
   не подходит для величин, которые не стабильны в начале (пустой список
   сотрудников заморозил бы ФОТ на 0 навсегда). Пересматривает решение
   «Pearl vs 1С» (закрыто 18.06) — теперь управленческая оценка в Pearl, не
   целиком зона 1С. Подробности — [[architecture/unit_economics_module]].
3. **Роль `founder`** заведена для Айдай (реальный бизнес-владелец,
   пароль `004`) — отдельно от `owner` (техдоступ Абдусаттара).
4. **Финэксперт разрешил нестыковку порогов МБП/ОС**: <10 000 сом —
   расход сразу (налоговый порог, НК КР ст.225); 10 000–50 000 — МБП/склад;
   >50 000 + служит >1 года — Основное средство. Дал сроки полезного
   использования по категориям (мебель/техника/инвентарь).
5. **Скидка родителям за пропуски решена** (минус 100 сом/день) — не
   реализована, см. [[architecture/billing_module]].
6. **UX-аудит по всему проекту** — найдены и исправлены 2 класса багов,
   существовавших до сессии: тап-зоны кнопок-крестиков (14×14px →
   24×24px+, WCAG 2.5.8) и iOS-автозум на полях количества/цены
   (font-size 12-13px → 16px на мобильном, самая частая форма —
   `expenses/confirm.html`). Сохранено правило: проверять это перед
   показом любого нового экрана впредь.
7. **Видение по масштабированию на других клиентов уточнено** (может
   случиться уже на следующей неделе) — два разных вопроса: (а) код
   школа/садик не разделяем, пока нет школьного кода; (б) новый клиент =
   отдельное развёртывание (своя БД), не строка в базе Жемчужины,
   платформа хостинга не обязательно та же. См. `project_brief.md`,
   раздел «Стратегическое видение — продолжение (10.07)».
8. **Ничего не задеплоено на прод** — только локально, Playwright + pytest
   без регрессий.

Полный список приоритетов на следующую сессию (гап-анализ юнит-экономики +
деплой) — см. «Первым делом в следующей сессии» ниже.

**Первым делом в следующей сессии (после 12.07):**
1. 🔴 Спроектировать «повторяющиеся ежемесячные расходы» (зарплата/охрана —
   шаблон+подтверждение; коммуналка — напоминание без суммы) — см. п.6 выше
2. 🟡 Разобрать оставшиеся 9 нечитаемых товаров на локалке по фото чеков
   (`receipts/2026-06/360dccfd924f.jpg`, `receipts/2026-07/e330eed2d8e2.jpeg`)
   — не блокирует ничего, просто хвост
4. Продолжают числиться с прошлых сессий (см. архив 10.07 ниже): сверка
   тарифов `monthly_fee`, скидка за пропуски, деплой меню+сотрудников на
   прод, реестр сотрудников (стартовый список от Мунары)

---

**Сессия 09.07 (обучение Махабат + параллельная разработка склада):**

Цель дня — Абдусаттар лично обучает Махабат вводу квитанций (на кыргызском,
частично онлайн) параллельно с разработкой в отдельном треке. Само обучение
не в зоне Claude — только материалы для него.

1. **Прод-БД садика Сокулук очищена от тестовых записей** перед обучением —
   полный JSON-бэкап снят перед удалением (org_id=4: квитанции, позиции,
   транзакции, складские приходы/списания). Тестовые платежи Optima
   (PIN 9001/9002) удалены как заведомо тестовые, временный каталог
   (12 позиций, is_standard=False) оставлен как есть.
2. **Складской модуль консолидирован** — было три места, где можно завести
   товар/движение в обход чека (`/warehouse/receipt/add`, форма в
   `/warehouse/products/`, старая однострочная актуализация); стало одно —
   актуализация с инлайн-созданием нового товара. Два роута и их шаблоны
   удалены полностью, не оставлены мёртвым кодом.
3. **Списание разделено по смыслу**: `/warehouse/writeoff/meal` — на приём
   пищи (дата+приём пищи, несколько товаров через автокомплит), обычное
   списание (`/warehouse/writeoff/add`) — порча/инвентаризация без привязки
   к еде. `children_count` убран из формы — было без источника данных.
4. **Посещаемость** (`/attendance/`) — по группам, отметка отсутствующих
   галочкой, снимает неопределённость «повар готовит на списочный состав
   или на факт» (регистрация до готовки → факт = прогноз). Вопрос скидки
   родителям за пропуски — явно НЕ решён этим, отдельный бизнес-вопрос к
   Мунаре/Айжан.
5. **Основные средства** (`/assets/`) и **ИНН поставщика** — по итогам
   консультации с финансовым экспертом (МБП-порог 50 000 сом, раздельный
   учёт Ремонта на Текущий/Капитальный, необходимость реестра ОС на
   будущее для амортизации, ИНН для налоговых регистров). Без проводок —
   у заказчика ещё нет 1С, только факт-регистрация.
6. **Актуализация — вторая волна доработок** (после протестировано в
   браузере Playwright, 0 ошибок консоли на каждом шаге):
   - Кнопка «Загрузить все текущие остатки» — предзаполняет таблицу
     остатками по системе (с явным `product_id` на каждой строке — иначе
     нечёткий поиск при сохранении рисковал привязать не к тому товару)
   - Группировка строк по категории (раньше сортировка шла только по
     имени — хозтовары и еда шли вперемешку)
   - Подсветка изменённой строки (сравнение с исходным загруженным
     значением, не с БД напрямую)
   - Явная подсказка: крестик = пропустить строку (остаток не меняется),
     0 = списать весь остаток — это два разных действия, легко перепутать
   - Категория «Бытовая химия» переименована в «Мыломойка» (миграция
     `x2y3z4a5b6c7`) — понятнее для персонала; не конфликтует с дублем от
     06.07 (та отдельная корневая «Мыломойка» была удалена, не эта)
   Подробности всех пунктов — [[architecture/warehouse_module]].
7. **Процессное правило от заказчика (повторено 3 раза за сессию)**:
   советоваться и показывать план ДО реализации, не после общего «да» на
   идею — не начинать код сразу.
8. **Бэклог-заметка**: отчёт по частоте/сумме корректировок актуализации
   (сигнал, что где-то раньше неверно внесли приход/списание) — данные уже
   размечены (`reason="инвентаризация"`), реализация отложена.
9. **7+2 коммита готовы, ещё не задеплоены на прод** на момент этой записи
   — деплой запланирован следующим шагом.

**Сессия 12.07, продолжение (Ежемесячные расходы + скидка + разделение Закупа):**

10. **Справочник «Ежемесячные расходы»** (`RecurringExpenseTemplate`) —
    ФОТ/Охрана/Коммуналка с подсказкой суммы (живой ФОТ сотрудников или
    последняя проводка), ручным подтверждением, `owner_only`-видимостью
    (только Абдусаттар/Талас/Айдай подтверждают ФОТ). Не строгая
    идемпотентность — аванс+остаток зарплаты допустимы двумя проводками в
    одном месяце. Экран `/expenses/recurring`, доступен кнопкой на
    `/expenses/`. Подробности — [[architecture/expenses_module]].
11. **Поле `Transaction.period`** — отдельно от `date`. `date` = когда
    реально оплачено, `period` = за какой месяц расход. Нужно, т.к. расход
    за июль иногда проводят в начале августа. Список на `/expenses/recurring`
    фильтрует по `period`, не по `date`. Проверено: проводка за июль с датой
    оплаты в августе видна именно в июльском списке, не в августовском.
12. **Скидка на тариф ребёнка** — 4 колонки на `Student`
    (`discount_percent`/`discount_reason`/`discount_set_by`/`discount_set_at`),
    расчёт в `generate_monthly_charges()`, `AuditLog` на изменение. **Форма
    редактирования на карточке ребёнка не дописана** — остановлено
    пользователем, отложено на следующую сессию, не горит.
13. **Консолидация категорий расходов** (дважды): Зарплата/Охрана/
    Коммуналка → «Ежемесячные расходы»; Транспорт/Прочее/Банковские
    комиссии → «Сервисные расходы». Разные расходы внутри «Сервисных»
    различаются описанием проводки (комментарием), не отдельными
    категориями — детализируем позже по факту частоты (заведём подкатегории
    когда станет ясно, что реально часто встречается).
14. **Два реальных бага найдены и исправлены**:
    - «Зарплата»/«Ежемесячные расходы» протекали в форму `/expenses/add`
      («Вручную») с бессмысленным обязательным «Поставщиком» и пустой
      таблицей позиций. Причина второй раз — `get_manual_form_categories()`
      фильтровала по старым именам категорий, не обновлённым после
      консолидации. Тест на эту утечку не проверял конкретно новое имя
      «Ежемесячные расходы» — стоит иметь в виду при следующих
      консолидациях категорий: тест должен проверять актуальное имя, не
      только отсутствие старых.
    - `resolve_org()` падал на первую организацию таблицы (Жемчужина),
      если явного `org_id` не было в URL (большинство ссылок меню) — из-за
      этого пользователи, переведённые на Садик Сокулук, видели «Школу» в
      выборе объекта. Fix: fallback на `user.organization_id` перед
      `accessible[0]`, в обеих копиях функции (`dependencies.py` и
      `routers/expenses.py` — известный дубль, не устранён).
15. **Разделение Закупа на три явных потока** — по запросу пользователя
    («у тебя должен быть расходы отдельно закупы, сервисы, ежемесячные»).
    Реализовано минимально безопасно: пункт левого меню «Ежемесячные»
    убран (был неочевиден сам по себе), «Закуп» переименован в «Расходы».
    На странице `/expenses/` три равноправные кнопки: «+ Вручную» (товары
    с позициями), «🚕 Сервис» (новое — преселект категории «Сервисные
    расходы» через `?category_id=`, позиции скрыты сразу), «📅 Ежемесячные».
    Полноценный редизайн (отдельные экраны для Сервисов, как у Ежемесячных)
    — сознательно не делался в ночь перед обучением, зафиксирован как
    направление на будущее.
16. **✅ PILOT_ORG_IDS решено (13.07).** Заменено на `PILOT_USER_IDS = {1,
    3, 61, 64}` (Абдусаттар/Мунара/Айдай/Талас) — ограничены Садиком
    Сокулук только эти четверо, не вся роль owner/founder/manager целиком.
    Любой другой owner/founder/manager видит все организации как обычно.
    Убрать конкретного человека из списка, когда он выходит из пилота.
17. **Деплой НЕ выполнен** — осознанно, по совету (пункт 9 выше уже
    накопил 7+2 незадеплоенных коммита; за вечер добавились ещё 2 коммита
    — `c3f0fad` Ежемесячные+скидка+консолидации, `442f678` кнопка Сервис).
    Прод сейчас работает на версии ДО этой сессии — рабочий вариант для
    завтрашнего обучения «как есть» (фото-квитанции + вручную). Деплоить
    всё сразу на свежую голову, не в ночь перед обучением — риск не
    успеть протестировать весь новый пакет (менюs+сотрудники+ежемесячные+
    скидка+две консолидации+period+org-fix+три потока) вместе.

**Первым делом в следующей сессии:**

0. 🔴 **Деплой на прод.** Порядок: применить все новые Alembic-миграции
   на проде (10 штук, последняя `m3n4o5p6q7r8`), затем код. Прогнать
   полный regression-тест по ролям после деплоя, как это делалось
   локально весь вечер.
0b. Проверить вживую на реальном обучении: понятны ли три кнопки
    «Вручную/Сервис/Ежемесячные» персоналу без объяснений — если нет,
    это сигнал делать полноценные отдельные экраны для Сервисов раньше,
    чем планировалось.

**Сессия 12.07 → 13.07, ночная часть (доводка Ежемесячных/Сервиса, ещё 5 коммитов):**

18. **ФОТ нельзя править/удалять напрямую** — только через оклады на
    /employees/. Остальные ежемесячные (Охрана/Электричество/Вода/
    Отопление/Интернет) — наоборот, можно и исправить, и удалить саму
    проводку (счёт пришёл раз в месяц, ошибся — поправил или удалил и
    провёл заново). Заблокировано и в UI, и на сервере (edit/delete
    редиректят на /employees/ для ФОТ-проводок).
19. **Одна проводка на месяц для не-ФОТ ежемесячных** — форма
    проведения скрывается после первой проводки, повторный POST в обход
    UI тоже блокируется на сервере. ФОТ не затронут — там по-прежнему
    можно провести несколько раз (аванс+остаток).
20. **Кнопка «Сервис» доведена**: категория зафиксирована (скрытый
    input, не select), поле «Поставщик» убрано полностью (таксисты и
    подобные меняются каждый раз, трекать как поставщиков смысла нет),
    комментарий стал обязательным и в форме, и на сервере — без
    поставщика это единственная зацепка, на что потратили. Заголовок
    страницы сменён на «Сервисный расход» вместо общего «Запись без
    квитанции».
21. **PILOT_ORG_IDS → PILOT_USER_IDS (закрывает п.16)** — ограничены
    Садиком Сокулук только 4 конкретных user_id (Абдусаттар/Мунара/
    Айдай/Талас), не вся роль owner/founder/manager. Реальный владелец
    (если заведётся отдельным пользователем не в этом списке) видит все
    организации как обычно. Блокер деплоя снят.

**Гап-анализ 10.07 — что не хватает для юнит-экономики (по приоритету):**
1. 🔴 **Свериться с Айжан/Мунарой по тарифам (`monthly_fee`)** — тянется
   уже 6+ сессий подряд. Без этого доходная часть юнит-экономики считается
   на неверных цифрах, независимо от точности расходной части. Это
   разговор, не разработка
2. 🔴 **Реализовать скидку за пропуски** (минус 100 сом/день, решено
   10.07) — без неё начисления (`Charge`) завышены на часто отсутствующих
   детей. Дизайн (`Charge` vs отдельная корректировка) — см.
   [[architecture/billing_module]], открытый вопрос
3. 🟡 Новые статьи расходов (Коммуналка, Охрана, Реклама) — чистая
   миграция категорий, 5 минут, не сделана
4. 🟢 **Отчёт «себестоимость на ребёнка» / Дашборд собственника** — все
   ингредиенты теперь есть (посещаемость, меню+списание по блюду, живой
   ФОТ/амортизация), самого отчёта нет. Делать **после** пп.1-2 — иначе
   дашборд с неверными входными цифрами создаёт ложную уверенность
5. Известное ограничение (не блокирует): история окладов не
   версионируется — ФОТ считается только «на сегодня», не ретроспективно

**Деплой (сегодняшняя сессия не задеплоена):**
6. Убедиться, что деплой 09.07 прошёл без проблем на проде — и задеплоить
   10.07: обе миграции (`d4e5f6g7h8i9` меню, `e5f6g7h8i9j0` сотрудники)
7. Продолжать следить за реальным вводом Махабат — включая новые `/menu/`
   и (если решим показать) `/employees/`
8. Отчёт по корректировкам актуализации склада — не сделан
9. Реестр сотрудников пуст — нужен стартовый список от Мунары (ФИО/роль/
   оклад), вопрос сформулирован в [[blueprints/unit_economics_module]]
8. Реестр сотрудников пуст — нужен стартовый список от Мунары (ФИО/роль/
   оклад по Сокулуку), вопрос сформулирован в
   [[blueprints/unit_economics_module]]

---

**Сессия 08.07:**

1. **Пилот только на садике Сокулук** — решение зафиксировано (см. «Принятые
   решения» ниже). Школа и Кожомкул не трогаются, пока Сокулук не обкатан.
2. **Автокомплит позиций перенесён на `add.html`** (ручная запись без фото) —
   раньше там был плоский `<datalist>`, теперь тот же fuzzy-компонент с
   `/expenses/products/search`, что на confirm-странице. Проверено end-to-end
   локально: выбор товара из подсказки резолвится в правильный `product_id`
   (не создаёт дубль), сумма считается сервером. Подробности —
   [[architecture/expenses_module]].
3. **Авто-промоут временного товара в эталон отменён полностью** (был по
   счётчику ≥3 повторов, `maybe_promote()` в `services/products.py`). Причина:
   Абдусаттар предпочитает решать сам, что становится эталоном, в диалоге с
   Claude по мере накопления временных товаров — риск, что опечатка или
   неудачный вариант написания зацементируется в каталоге после 3 повторов,
   был признан важнее удобства автоматизации. Функция удалена из кода (не
   оставлена мёртвой). Проверено на dev: один и тот же новый товар отправлен
   4 раза — остался `is_standard=False`. Подробности — [[architecture/expenses_module]].
4. Восстановлена задним числом фиксация сессии 07.07 (была пропущена) — см.
   архив ниже.
5. **Пункты 2-3 задеплоены на прод** (`2462cb9`, проверено — `railway status`
   показывает `SUCCESS`/`RUNNING`, `/login` отвечает 200). Дальше Махабат
   работает уже с новой версией.
6. **Продиктовано и записано стратегическое видение** (роллаут на Сокулуке,
   переосмысление «Меню → Склад», капитальные статьи, полная картина расходов
   для юнит-экономики, UX-принципы) — `context/project_brief.md`, раздел
   «Стратегическое видение — продолжение (08.07)». Закоммичено отдельно
   (`4704103`).

**Первым делом в следующей сессии:**
1. **Задать Мунаре/Айжан вопрос про посещаемость отсутствующих детей** — (а)
   делают ли скидку/вычет по оплате за дни отсутствия, (б) повар готовит на
   списочный состав группы или на факт пришедших. Ответ определяет модель
   данных для будущего меню-модуля (`WriteOff.children_count` по списку vs
   по факту, нужен ли вообще вычет в `Charge`) — решить до того, как
   начинать проектировать меню-модуль, не после
2. Периодически свести список временных товаров (`is_standard=False`) и
   вместе с Абдусаттаром решить, что промоутить в эталон вручную — теперь
   единственный путь роста каталога
3. Продолжить следить за реальным вводом Махабат/Мунары
4. Разобрать новые фото чеков в `media/` (не загружены в OCR-пайплайн)
5. Дождаться результата тестов Юрия по Optima
6. Дождаться ответа финансового эксперта по категориям расходов (мебель/
   стройматериалы/ремонт/амортизация — вопрос уже расширен 08.07, см.
   project_brief.md)
7. Свериться с Айжан/Мунарой — тарифы `monthly_fee` (уже 6-ю сессию не сверено)

---

## Архив: сессия 07.07

**Сессия 07.07 (весь день — денежная логика позиций доведена до строгой):**

Как только Мунара/Махабат начали реально вводить чеки с новой авторазбивкой
категорий (введённой 06.07), всплыла системная дыра: несколько мест в коде
всё ещё тихо доверяли числам напрямую из OCR вместо пересчёта из проверенных
человеком полей — ровно то, от чего должна была защищать итемизация. Пять
последовательных фиксов, каждый вскрывал следующий слой той же проблемы
(OCR-промпт под реальные форматы чеков → сервер не доверяет присланному
total, всегда qty×price → скрыты позиции, не считающиеся товаром → их сумма
не размазывается по категориям, а честно уходит в «без категории» → в итоге
даже это убрано: для нераспознанных строк денег не остаётся нигде, только
счётчик). Полная хронология и правило на будущее — [[architecture/expenses_module]].

Сессия не была зафиксирована сразу (`logs/terminal/2026-07-07.md` остался
пустым) — восстановлено из git log и транскрипта задним числом 08.07.

**Первым делом в следующей сессии:**
1. Продолжить следить за реальным вводом Махабат/Мунары — эта цепочка багов
   нашлась именно на живом использовании, возможны ещё слои
2. Разобрать новые фото чеков в `media/` (не закоммичены: `media/rec 0607/`,
   `media/receipts_food/`, несколько одиночных файлов в корне) — не
   загружены в OCR-пайплайн
3. Дождаться результата тестов Юрия по Optima — если ок, договор двигается дальше
4. Дождаться ответа финансового эксперта по категориям расходов
5. Решить, нужна ли итемизация для Транспорта/Прочего (отложено 06.07)
6. Свериться с Айжан/Мунарой — актуальны ли тарифы `monthly_fee` (уже 6-ю
   сессию не сверено)
7. Разобрать оставшиеся открытые позиции каталога: «Лесна», «Кабель-канал»
8. Заполнить `monthly_fee`/группы для детей без них

---

## Архив: сессия 06.07

**Сессия 06.07 (на объекте, обучение персонала + каталог + Optima):**

1. **Заведён аккаунт Махабат** — role=`staff` (переименовано из `teacher`, само название
   вводило в заблуждение), org_id=4 (Садик Сокулук), доступ только к закупу/квитанциям
   этого объекта. Пароль `003` (схема как у остальных — см. `scripts/set_passwords.py`
   и `scripts/add_user.py`).
2. **Каталог товаров расширен с 39 до 101 позиции** — раньше был только food, теперь
   +стройматериалы (30), +бытовая химия (10), +игрушки (5), +инвентарь (2, батарейки/вилки),
   +напитки (1, генерик «Напиток газированный» для Пепси/Кола на будущее). Прошёлся по
   ВСЕМ позициям из 109 отсканированных чеков (не только тем, что встречались ≥2 раза —
   раз чеки уже на руках, дожидаться повтора смысла нет). Принцип: фиксируем вид товара,
   не марку/сорт («цемент», не «цемент М400»), но разводим по смыслу разное («болгарский
   перец» ≠ «перец чёрный»). Подробности и список нерешённых позиций (Лесна, Кабель-канал,
   Кровать 4х ярусная) — [[architecture/expenses_module]].
3. **Категории расходов почищены**: убрана дублирующая «Мыломойка» (добавлена 29.06,
   0 транзакций, забытый хвост), добавлена «Игрушки» под «Хозяйство». Миграция
   `o3p4q5r6s7t8`.
4. **confirm.html — UX проверки позиций упрощён**: было 4 цветовых состояния (зелёный/
   жёлтый/оранжевый/красный по типу AI-совпадения) — персонал (и сам Абдусаттар) путался.
   Свели к 2 (зелёный «готово» / жёлтый «проверь») + счётчик «N строк требуют проверки».
   Решение по совету эдвайзера: сотруднику нужно бинарное «доверять или проверить», не
   4-уровневая уверенность. На период обучения — строго (даже уверенное AI-совпадение
   помечается «проверь»). Подробности — [[architecture/expenses_module]].
5. **Optima `check` дополнен объектом**: теперь `ФИО, Объект Жемчужина, группа X` вместо
   `ФИО — Группа`. Тестовые аккаунты 9001/9002 получили реалистичные ФИО и группы.
   Задеплоено (`5b0fe8e`), протестировано на проде: check/pay/идемпотентность/ошибки —
   всё ок. **Юрию (тех.специалист Optima) передано** тестовое сообщение с эндпоинтом,
   PIN, форматами запросов, кодами ошибок — ждём его прогонов.
6. **Отправлен вопрос финансовому эксперту** (NotebookLM) — по учёту всех видов товарных
   расходов (игрушки/стройматериалы/химия/канцтовары/транспорт): МБП vs ОС vs текущий
   расход. Ответ ещё не получен — см. `context/finance_expert_prompt_categories.md`.
7. **Два recurring бага найдены и починены**: (а) `scripts/scan_receipts.py` сравнивал
   по абсолютному Windows-пути вместо относительного — не узнавал уже отсканированные
   чеки; (б) sequence `users_id_seq` разъехалась и на **проде** (не только в dev, что
   чинили 04.07) — тот же класс проблемы, ручной сидинг без `setval`. Способ гонять
   разовые скрипты на прод-БД без зависания — [[architecture/deploy_ops]].

**Продолжение сессии 06.07 (та же сессия, после первой фиксации выше) — крупная переделка
модуля расходов, живой диалог с заказчиком по ходу работы:**

8. **Кредиторская задолженность реализована полностью**: `amount_paid`/`due_date` на
   Transaction (миграция `p4q5r6s7t8u9`), чекбокс «Оплачено полностью» (по умолчанию
   включён) на confirm/add/edit, счётчик «Долг поставщикам» на `/expenses/` и
   `/suppliers/`. Убраны неиспользуемые поставщики «Магазин»/«Магазин Народный» (общие
   покупки → «Рынок»), убрано инлайн-добавление поставщика в момент чека — теперь только
   заранее через `/suppliers/`, выбор поставщика стал обязательным для проведения.
9. **Категория расхода теперь полностью автоматическая** — самая крупная переделка.
   Убрано ручное поле «Категория» из confirm.html и add.html. Добавлен
   `products.expense_category_id` (миграция `q5r6s7t8u9v0`, бэкфилл по мэппингу
   мелкая→крупная категория, проверено на реальных данных перед деплоем). Позиции чека
   группируются по категории товара, сумма/оплата/долг делятся пропорционально между
   получившимися транзакциями. **Проверено end-to-end на реальном чеке #4** (3044 сом,
   3 категории, частичная оплата 2000) — суммы сошлись до копейки. Неопознанные позиции
   идут с категорией NULL (не блокирует), счётчик «Без категории» на списке расходов.
   Подробности — [[architecture/expenses_module]].
10. **Заказчик (Айжан/Мунара) потребовали обязательную итемизацию везде**, включая
    Ремонт (стройматериалы) — для юнит-экономики. `add.html` теперь показывает позиции
    для всех категорий кроме Транспорта/Прочего (отложены как услуги). Ремонт убран из
    списка «не размножать в эталонный каталог автоматически».
11. **Кнопка «OCR ошибся»** на confirm.html — принципиально НЕ пропускает итемизацию
    (по прямому требованию заказчика), только очищает мусорные строки и даёт вписать
    заново вручную.
12. **AI предлагает стандартизированное имя** для позиций без совпадения в каталоге
    (`normalize.py` → `clean_name`) — снижает риск, что неопытный сотрудник насорит в
    каталог случайными вариантами написания. Не выдумывает имя, если строка вообще не
    товар. Финальное решение — всегда за человеком (объяснение риска — почему нельзя
    автопринимать — в wiki).
13. **UX успех-экрана**: «+ Следующая квитанция» стала единственной крупной кнопкой
    (по факту — единственный реальный следующий шаг цикла массового ввода, доказано
    отсутствием bulk/бот-загрузки в системе), «Непроведённые»/«К списку» — мелкие
    второстепенные ссылки.
14. **Производительность `/expenses/`**: было 4.2с (N+1 — организации запрашивались
    4 раза за рендер, транзакция каждой квитанции — отдельным запросом в цикле), стало
    ~2.6с. Остаточная разница с базовой линией других страниц (~1.3с) не исследована
    до конца — можно копать дальше при следующей возможности.

**Первым делом в следующей сессии:**
1. Последить за новой авторазбивкой по категориям на реальном вводе Махабат/Мунары —
   не путает ли их отсутствие поля «Категория»
2. Решить, нужна ли итемизация для Транспорта/Прочего (отложено как «сервисы» 06.07)
3. Дождаться результата тестов Юрия по Optima — если ок, договор двигается дальше
4. Дождаться ответа финансового эксперта по категориям расходов — при необходимости
   поменять структуру (МБП-учёт для игрушек/стройки?)
5. Разобрать оставшиеся открытые позиции каталога: «Лесна» (неизвестный товар),
   «Кабель-канал» (слит с «Гофра» — развести?)
6. Донастроить `expense_category_id` для товаров, у которых пусто (счётчик «Без
   категории» на `/expenses/` покажет сколько)
7. Свериться с Айжан/Мунарой — актуальны ли тарифы `monthly_fee` (уже 5-ю сессию не
   сверено)
8. Заполнить `monthly_fee` для 12 новых детей без тарифа, группы для 16 детей без группы
9. Разобрать `media/` — новые фото чеков/WhatsApp (несколько файлов от 29.06, ещё не
   загружены в OCR-пайплайн)
10. Опционально: докопать остаточную медленность `/expenses/` (~2.6с vs ~1.3с базовая
    линия других страниц)

---

## Архив: сессия 05.07

1. **Задеплоена сессия 04.07** — коммиты `8045b77`/`6389837` были закоммичены
   локально, но не запушены. Запушены на `origin/master` → Railway задеплоил
   автоматически, миграция `n2o3p4q5r6s7` применена (через `alembic upgrade
   head` в `entrypoint.sh` при старте контейнера). Подтверждено напрямую:
   `/students/{id}/history` на проде отдаёт 302 (не 404) — новый код живёт.
2. **Разобран многонедельный хвост незакоммиченных файлов** в рабочей
   директории (каталог продуктов 130+ позиций, PIN-скрипты, разбор реальных
   названий с чеков — работа сессий 19.06–29.06, никогда не коммитилась).
   Закоммичено `data/`+`scripts/`+`context/mode.md`+`optima_api_summary.md`
   отдельным chore-коммитом (`f56296d`). `raw/primary_data.md` **восстановлен**
   (правило «raw/ неизменяем» — деletion был случайным). Permissions
   (`Bash(*)`, `Write(*)` и т.д.) перенесены из общего `settings.json` в
   личный `settings.local.json` — не должны жить в shared-репо. Устаревшие
   `PEARL_*.md` (UX-аудит от 29.06, уже реализован и в git log) — удалены.
   `context/tasks.md` — правка отменена, формат устарел, summary.md остаётся
   единственным источником статуса. `media/` (фото чеков/WhatsApp,
   `Снимок.PNG`) — оставлены нетронутыми, не разобраны.
3. **OCR-модель — сравнение альтернатив.** Абдусаттар не устраивала скорость
   прода — заодно проверили, есть ли более подходящая по цене/качеству
   vision-модель взамен `google/gemini-2.5-flash-lite`. Прогнали
   `qwen/qwen3-vl-32b-instruct` и `z-ai/glm-4.5v` на двух реальных
   рукописных чеках. **Решение: остаться на Gemini 2.5 Flash Lite** — ни
   один китайский кандидат не показал превосходства (Qwen: числа
   правдоподобнее на нечитаемом чеке, но хуже на чётком; GLM-4.5V:
   «thinking»-модель, обрезалась по `max_tokens=2048`, на одном чеке вообще
   вернула пустой ответ). Подробности и как повторить тест —
   [[ocr/decision]].
4. **Найдена и исправлена реальная причина проблем на проде** (не скорость,
   а падения): `app/routers/expenses.py` — 9 из 10 обработчиков вызывали
   `get_current_user()` без проверки на `None` (в отличие от всех остальных
   роутеров). При истёкшей сессии это давало `500 Internal Server Error`
   вместо редиректа на `/login` — в метриках Railway это выглядело как 31%
   5xx за последний час. Исправлено, проверено локально и на проде (все
   ранее падавшие роуты теперь отдают 302), задеплоено (`5865aa2`). Railway
   при этом оказался на тарифе **`trial`** (shared vCPU, 1GB RAM) — ресурсов
   хватало с большим запасом (CPU ~0%, память 10% от лимита), так что это
   **не** было причиной проблемы, но тариф стоит проапгрейдить при росте
   нагрузки. Подробности — [[architecture/deploy_ops]].

**Первым делом в следующей сессии:**
1. Последить день-два — снялась ли жалоба на «медленность» после фикса
   auth-бага; если нет — тогда смотреть тариф Railway/регион
2. Свериться с Айжан/Мунарой — актуальны ли тарифы `monthly_fee`
   (непроверено уже 4-ю сессию подряд, влияет на все балансы в `/reports/`)
3. Дождаться результата тестовых платежей от банка (PIN 9001/9002)
4. Заполнить `monthly_fee` для 12 новых детей без тарифа
5. Заполнить группы для 16 детей без группы
6. Разобрать `media/` — новые фото чеков/WhatsApp, не загружены в OCR-пайплайн
7. Статус оплаты на закупе (кредиторка) — старый пункт, ещё не сделан

---

## Архив: сессия 04.07

Сессия 04.07 (длинная, локально на Windows-машине, не Railway): полный проход
по юзабилити карточки ребёнка и связанных экранов. Карточка `/students/{id}/edit`
разбита на независимые блоки «Личные данные»/«Оплата» с режимом
просмотр→«Изменить»→сохранить; баланс и история платежей переехали на
отдельную страницу `/students/{id}/history`; ФИО ребёнка структурировано
(Фамилия/Имя обязательные, Отчество нет); статус (Активен/Выбыл) стал
обратимым полем на карточке; «Ручная корректировка» баланса убрана целиком
(риск бесконтрольного списания долга). Подробности решений — в
[[architecture/billing_module]].

После этого — стратегическое обсуждение (не про код): куда движется Pearl
как продукт, не только как проект для одного клиента. Зафиксировано в
`context/project_brief.md` (раздел «Стратегическое видение»). Заодно
подтверждён и отправлен банку (Юрий, тех.специалист Optima) адрес интеграции
и схема «1 эндпоинт на 3 услуги» — см. блок оплат ниже.

**⚠️ ВАЖНО — непроверенные данные, актуально уже 3-ю сессию подряд:** долг на
`/reports/` посчитан по тарифам (`monthly_fee`), которые **всё ещё не
сверялись с Айжан/Мунарой**. Прежде чем показывать эти цифры кому-то как
факт — свериться.

**✅ Задеплоено 05.07** — коммиты запушены, миграция `n2o3p4q5r6s7` применена
на проде автоматически (см. раздел «Где остановились» выше).

**Незакрытое из аудита usability 04.07** (см. `logs/terminal/2026-07-04*` за деталями):
- `/expenses/confirm` — обсуждали, что форма визуально плотная и неинтуитивная, конкретных правок ещё не решили
- Нижнее мобильное меню всё ещё без «Поставщики»/«Услуги»
- Инлайн quick-add (новый поставщик/продукт прямо в форме) — паттерн подтверждён, но не обсуждали до конца
- `context/ideas.md` — есть неразобранная идея про цены на услуги по объектам (школа/садики)

**На неделе:**
- Добавить в систему Кожомкул + школу (пока в базе только садик Сокулук)
- Уточнить у Амана/Марселя, показывает ли приложение Optima родителю ФИО+группу из ответа `check` перед оплатой
- Наполнить справочник `/services/` реальными доп.услугами (проезд, секции) и ценами

## Принятые решения

- **Пилот только на садике Сокулук (08.07):** Мунара, Абдусаттар, Махабат работают
  в системе только по объекту «Садик Сокулук», пока он не обкатан. Школу и садик
  Кожомкул сознательно не добавляем в систему до этого момента (сейчас их и так
  ещё нет в базе — см. «Цифры» ниже). После того как Сокулук обкатан — расширяться
  на остальные объекты.


- **PIN ребёнка:** формат 0001–9999 (4 цифры, никогда не переиспользуется — см. `app/services/students.py`). Сменили с 3 на 4 знака 03.07, пока родители ещё не пользовались боевым PIN — миграция была бесплатной. Причина: при 3 знаках и ~514 детей на всю организацию (школа+сады) запаса не хватило бы на пару лет текучки.
- **Долги/переплаты:** один общий баланс на ребёнка (не по видам услуг) — протокол банка не передаёт назначение платежа, родитель просто платит сумму. Учёба — индивидуальный тариф (`Student.extra.monthly_fee`), доп.услуги (проезд/секция) — справочник `Service` + чекбоксы на ребёнке (`StudentService`), начисляются вместе раз в месяц (`Charge`, генерируется при заходе на `/students/` или `/reports/`, без cron). **Ручной корректировки больше нет** (убрана 04.07 — см. ниже и [[architecture/billing_module]]).
- **Структура интерфейса (IA), обновлено 04.07:** Ученики = только личные данные + статус (PIN/ФИО/группа/Активен-Выбыл); карточка ребёнка (`/students/{id}/edit`) = ДВА независимых блока с раздельным сохранением — «Личные данные» и «Оплата», деньги/история сюда больше не входят; `/students/{id}/history` = баланс + единая лента начислений/оплат; `/reports/` = сводный долг/переплата по всем, фильтр по группе + «Только с долгом». Правило: у каждого вида данных — одно чёткое место, не дублировать по страницам. Подробная архитектура — [[architecture/billing_module]].
- **Просмотр/редактирование как режимы (04.07):** формы, которые правятся редко, но смотрятся часто (карточка ребёнка), открываются как текст, кнопка «Изменить» → редактируемые поля → «Сохранить» с подтверждением или «Отмена» без запроса на сервер. Причина: Айжан/Мунара нетехнические, боятся случайно испортить данные.
- **ФИО ребёнка (04.07):** разбито на `last_name`/`first_name` (обязательные) + `patronymic` (поле есть, не обязательно) — составное `name` сохраняется и пересчитывается на каждое сохранение для обратной совместимости (поиск, Optima, списки не трогали). Заодно добавлено `parent_name` (имя родителя, отдельно от телефона).
- **Банки:** Optima интеграция работает, банк сейчас тестирует. URL: `https://pearl-production-eef5.up.railway.app/optima/payment` — передан Аману/тех.специалисту, вместе с тестовыми PIN 9001/9002 (фиктивные аккаунты, не реальные дети)
- **Бакай API:** подтверждено Айжан (05.06) — QR оплаты, выписка, остаток
- **ИС стек:** FastAPI + PostgreSQL + HTMX/Jinja2 + OpenRouter Vision OCR + Docker → Railway
- **OCR:** Gemini 2.5 Flash Lite через OpenRouter
- **Авторизация:** bcrypt напрямую (не passlib) — passlib конфликтует с bcrypt на Windows
- **Пароли:** Абдусаттар=000, Айжан=001, Мунара=002, Махабат=003, Айдай=004, Талас=005 (установлены и в Railway DB, и локально — `scripts/add_user.py`)
- **org_id тестировщиков (12.07):** Абдусаттар/Мунара/Айдай/Махабат/Талас = 4 (Садик Сокулук, объект пилота), Айжан = 2 (Школа, реальная роль). owner/founder/director видят все организации — org_id только дефолтный контекст при входе, не ограничение доступа (`get_accessible_orgs()` в `app/dependencies.py`)

### Архитектура Pearl vs 1С (закрыто 18.06)

| Система | Владеет | Кто вводит |
|---|---|---|
| **Pearl IS** | Доходы (Optima webhook), Расходы (OCR чеков), Физический склад (кг, порции) | Мунара, Айжан, повар/завхоз |
| **1С** | Зарплата (начисление, НДФЛ, Соцфонд), Официальная отчётность | Бухгалтер |
| Передача | Pearl → бухгалтер ежемесячно: реестр реализаций, авансовый отчёт, акты ТМЗ, ведомость взаиморасчётов | До 1-го числа следующего месяца |

**Авансы от родителей = обязательство** (не выручка) до момента оказания услуги.

## Что реализовано в БД и коде

| Модуль | Статус |
|---|---|
| Organizations, Users, Groups, Students | ✅ schema + seed |
| Receipts + OCR (Gemini) + receipt_items | ✅ |
| Transactions (income + expense) + ReceiptTransaction | ✅ |
| ExpenseCategory + `warehouse_eligible` флаг | ✅ (18.06) |
| Категории: Питание / Хозяйство / Транспорт / Ремонт / Прочее + подкатегории | ✅ |
| Products + ProductAlias + ReceiptItems | ✅ |
| **Эталонный каталог 130 продуктов** (халяль, КР, кыргызские алиасы) | ✅ (19.06) |
| **AI-нормализация** OCR → каталог через Gemini (normalize.py) | ✅ (19.06) |
| WarehouseReceipt + WriteOff (склад) | ✅ |
| **Авто-склад из квитанции** (warehouse_eligible → чекбокс на confirm) | ✅ (18.06) |
| Веб: upload / confirm / list / add расходов | ✅ |
| Веб: /income/ список + Optima badge | ✅ |
| Веб: /warehouse/ остатки / приход / списание / продукты | ✅ |
| Аутентификация (bcrypt, сессии) | ✅ |
| Optima webhook `/optima/payment` | ✅ (тест прошёл 17.06) |
| **Мобильный UX**: адаптивные таблицы, Inter шрифт, топбар | ✅ (29.06) |
| **Переименование**: Расходы → Закуп (UI only, URL `/expenses/` не менялся) | ✅ (29.06) |
| **Логотип**: реальное фото в топбаре (`/static/logo.png`) | ✅ (29.06) |
| **Поставщики**: справочник + привязка к транзакции + колонка в списке | ✅ (29.06) |
| **Группы** (садик Сокулук): 5 групп, 80 детей распределены, 12 новых заведены — синхронизировано dev+prod | ✅ (03.07) |
| **Редактирование ученика**: ФИО, группа (с историей), тариф, услуги | ✅ (03.07) |
| **Авто-архивация**: через год после «выбыл» → статус archived, без удаления | ✅ (03.07) |
| **Биллинг**: начисления (Charge), доп.услуги (Service/StudentService), баланс долг/переплата | ✅ (03.07) |
| **Фильтр по группе** на списке учеников и в отчёте | ✅ (03.07) |
| **`/reports/`**: сводная таблица долгов/переплат по всем детям + фильтр «Только с долгом» | ✅ (04.07) |
| Optima `check` показывает родителю ФИО + группу + объект (школа/садик) | ✅ (03.07, объект добавлен 06.07) |
| **Карточка ребёнка разбита**: «Личные данные»/«Оплата» — раздельные блоки, просмотр→«Изменить»→сохранить | ✅ (04.07) |
| **`/students/{id}/history`**: баланс + единая лента начислений и оплат (Optima/Вручную бейджи) | ✅ (04.07) |
| **Статус ребёнка обратим**: select Активен↔Выбыл на карточке, группа закрывается/переоткрывается | ✅ (04.07) |
| **Ручная корректировка баланса убрана** (была без ограничения роли и без created_by) | ✅ (04.07) |
| **ФИО структурировано**: last_name/first_name/patronymic + parent_name; миграция `n2o3p4q5r6s7` | ✅ (04.07) |
| **`/income/` → «Оплаты»**: колонка баланса, клик по строке → история ребёнка | ✅ (04.07) |
| Исправлен критичный баг: `POST /students/add` падал (`get_mock_user` не существовал) | ✅ (04.07) |
| Тесты: pytest (5/7, 2 упавших не связаны — test_products, дефект до 04.07); тестовая инфра (`conftest.py`) починена — `db.commit()` внутри теста больше не пробивает rollback | ⚠️ |
| **Каталог товаров расширен на нефудовые категории**: стройматериалы/бытовая химия/игрушки/инвентарь/напитки (39→101 позиций) | ✅ (06.07) |
| **confirm.html — 2-состояния вместо 4** (зелёный/жёлтый + счётчик непроверенных строк) | ✅ (06.07) |
| **Аккаунт Махабат** (role=staff, доступ только к закупу Сокулука) | ✅ (06.07) |
| **Кредиторская задолженность**: amount_paid/due_date, чекбокс «Оплачено полностью», счётчик долга | ✅ (06.07) |
| **Категория расхода — полностью автоматическая** из товара (expense_category_id), авторазбивка + пропорциональный долг | ✅ (06.07) |
| **Позиции обязательны везде** кроме услуг (Транспорт/Прочее) — включая Ремонт | ✅ (06.07) |
| **AI предлагает стандартизированное имя** для новых позиций (normalize.py → clean_name) | ✅ (06.07) |
| **Фикс N+1 на `/expenses/`**: 4.2с → 2.6с | ✅ (06.07) |
| **Денежная логика позиций доведена до строгой**: сервер всегда qty×price (не доверяет OCR-сумме), нераспознанные строки не размазывают деньги по категориям и не попадают в проводку вообще | ✅ (07.07) |
| **Автокомплит позиций на ручной записи** (`add.html`) — тот же fuzzy-поиск по каталогу, что на confirm.html, вместо плоского datalist | ✅ (08.07) |
| **Авто-промоут товара в эталон отменён** — теперь только вручную (Абдусаттар + Claude по мере накопления временных товаров) | ✅ (08.07) |
| **Склад консолидирован**: `/warehouse/receipt/add` и форма добавления в `/warehouse/products/` удалены, единственный путь создания нового товара — актуализация | ✅ (09.07) |
| **Списание на приём пищи** отделено от обычного списания (порча/инвентаризация) | ✅ (09.07) |
| **Посещаемость** (`/attendance/`) — отметка отсутствующих по группам | ✅ (09.07) |
| **Основные средства** (`/assets/`) + **ИНН поставщика** | ✅ (09.07) |
| **Актуализация**: кнопка загрузки текущих остатков, группировка по категории, подсветка изменённых строк | ✅ (09.07) |
| **Меню/приёмы пищи**: `Dish`/`MenuEntry`, `/menu/` (карточки по дням, чипы «обычно даём»), `WriteOff.dish_id` на списании | ✅ (10.07) |
| **Сотрудники/ФОТ/Амортизация**: `Employee`, `/employees/`, `Asset.useful_life_months`, живой расчёт (не Transaction), роль `founder` (Айдай) | ✅ (10.07) |

## Модуль Поставщиков — архитектура (29.06)

- Таблица `suppliers`: id, name, phone (nullable+unique), created_at — глобальная (без org_id)
- `supplier_id` FK на `transactions` (не на receipts — manual expenses без items не создают Receipt)
- Seed: Рынок, Магазин Народный, Магазин
- Dropdown на confirm.html и add.html с inline "Добавить нового..." (JS toggle)
- `resolve_supplier()` в expenses.py — идемпотентен по имени
- Колонка "Поставщик" в списке закупов (серым если не указан)
- CRUD страница `/suppliers/`

## Модуль Биллинга — архитектура

Полная актуальная архитектура (модель данных, IA, известные риски) —
[[architecture/billing_module]]. Кратко: `Charge` (только автоматическое
начисление, ручной корректировки нет с 04.07) − `Transaction(type=income)` =
баланс. Генерация начислений без cron, при заходе на `/students/` или
`/reports/`.

## Логика авто-склада (важно)

При подтверждении квитанции:
- Категория `warehouse_eligible = true` (только «Продукты питания») → показывается зелёный чекбокс «Добавить в склад»
- По умолчанию включён. При Провести → `WarehouseReceipt` создаётся для каждой позиции с qty и price
- Категория «Готовая еда» → `warehouse_eligible = false` → чекбокс не показывается → идёт прямо в расход
- `WarehouseReceipt.transaction_id` связывает складской приход с расходной транзакцией

## Структура категорий расходов

| Родительская | Дочерние | warehouse_eligible |
|---|---|---|
| Питание | Продукты питания | ✅ true |
| Питание | Готовая еда | false |
| Питание | Питьевая вода | false |
| Хозяйство | Мыломойка (была «Бытовая химия», переим. 09.07) | false |
| Хозяйство | Игрушки | false |
| Хозяйство | Канцтовары | false |
| Транспорт | — | false |
| Ремонт | — | false |
| Прочее | — | false |

С 06.07 у Ремонта тоже есть свой каталог товаров (стройматериалы) и авто-промоут
временных позиций в эталон — раньше был только у Питания.

Категории ФОТ/Амортизация — **не в Pearl** (это 1С).

## Бэклог — следующие модули (приоритет)

### ~~1. Статус оплаты поставщику (Кредиторская задолженность)~~ — ✅ сделано 06.07
`amount_paid`/`due_date` на Transaction, чекбокс «Оплачено полностью», счётчик долга
на `/expenses/` и `/suppliers/`. Подробности — [[architecture/expenses_module]].

### 2. Грумминг каталога продуктов
- Страница `/expenses/catalog/review` — список временных продуктов
- Промоут в эталон (is_standard = true) или объединение с существующим
- Донастроить `expense_category_id` там, где пусто (счётчик «Без категории» на `/expenses/`)

### 3. Меню → Склад (Расход продуктов) — сложный, большой проект

**Переосмыслено 08.07** — не жёсткий рецепт с граммовками заранее, а прогноз
vs факт: меню на неделю + число детей → ожидаемый расход; повар при готовке
пишет фактический расход по приёму пищи (завтрак/обед отдельно); система
показывает расхождение ожидание↔факт, не блокирует. Та же логика адекватности
(норма по периодичности vs факт) — и для хозтоваров. Подробности —
[[architecture/warehouse_module]] и `context/project_brief.md` (раздел
«Стратегическое видение — продолжение 08.07»).

## Блок оплат — статус

| Вопрос | Статус |
|---|---|
| Optima webhook реализован (check+pay, протокол OSMP/QIWI) | ✅ |
| Тестовый платёж через Optima прошёл (старый 3-знач. PIN) | ✅ (17.06) |
| Банк начал реальное тестирование по протоколу | 🔄 в процессе (03.07) |
| URL + тестовые PIN 9001/9002 переданы банку | ✅ (03.07) |
| Тех.специалист банка (Юрий) уточнял адрес и схему «1 эндпоинт на 3 услуги» — подтверждено и отправлено ему в Telegram 04.07 | ✅ (04.07) |
| Юрию переданы тестовые данные для прогонов check/pay (эндпоинт, PIN 9001/9002, форматы, коды ошибок) | ✅ (06.07) |
| Банк реально гоняет тесты check/pay (13-14.07): успешный платёж, платёж с копейками (35.50), сценарий «ребёнок выбыл» (код 79) | ✅ все пройдены |
| Лог всех попыток Optima (успех/отказ) — `/optima/log`, видно только Абдусаттару | ✅ (14.07) |
| Копейки в суммах — банк усомнился, на деле хранились точно, поправили только отображение в UI | ✅ (14.07) |
| Таймзона сервера/БД — было UTC, банк заметил расхождение со временем Бишкека — переведено на Asia/Bishkek | ✅ (14.07) |
| `txn_date` от банка в ISO 8601, наш парсер ждёт `YYYYMMDDHHMMSS` — падает молча, подставляется today() | ❓ мелкий хвост, не исправлено |
| Договор с Optima подписывается | 🔄 в процессе |
| PIN-список нужно переотправить банку (сменился формат на 4 знака) | ❓ |
| Открытый вопрос банку (не задан ещё): показывает ли приложение Optima родителю ФИО+группу из ответа `check` реально | ❓ |

## Цифры

- **Школа:** ~19 классов × ~22 ребёнка = ~418 учеников (0-4 класс) — ещё не в системе
- **Садик Сокулук:** 96 детей в системе (84 исходных + 12 новых из ИНН(2).xlsx), 5 групп
- **Садик Кожомкул:** пока не в системе
- **Тариф (monthly_fee):** известен у 84 исходных детей Сокулука; 12 новых (добавлены 03.07 из ИНН(2).xlsx) — без тарифа, дозаполнить вручную на `/students/{id}/edit` (у 2 из них тариф есть в исходном файле, не перенесён — можно взять оттуда)

## Стейкхолдеры

| Роль | Имя | Как работает с ИС |
|---|---|---|
| PM/разраб | Абдусаттар | Терминал + Claude Code |
| Собственник | Айдай | Веб — финансы, оклады (`role=founder`, заведена 10.07) |
| Собственник | Талас | Веб (`role=owner`, заведён 12.07) |
| Директор школы | Айжан Акматбековна | Веб — отчёты + расходы |
| Управляющая садиками | Мунара Кубатовна | Веб + склад + дети |
| Optima (банк) | Аман (Марсель) | Интеграция webhook |

## Локальная разработка

- **Docker не установлен на машине** (проверено 12.07, `docker`/Docker Desktop отсутствуют) — `docker-compose.yml` в репо нерабочий вариант для этой машины. Реально используется: Postgres как нативный Windows-сервис (`postgresql-x64-16`, `localhost:5432`, БД `pearl`) + `python -m uvicorn app.main:app --host 0.0.0.0 --port 8001` напрямую (зависимости стоят в системном Python, не venv).
- Postgres на этой Windows-машине уже поднят (`localhost:5432`, БД `pearl`) — можно гонять `uvicorn app.main:app --reload --port 8000` локально без Railway/Docker для быстрой проверки (порт 8000 или 8001 — оба использовались, не принципиально)
- **Готча (кириллица):** bash/curl в этой среде коверкает кириллицу — и на входе (`--data-urlencode` с русским текстом реально портит данные в БД), и **на выходе** (print() кириллицы из Python-скрипта в консоль показывает `�`, хотя сами данные корректны). Для HTTP-тестов с кириллицей — писать `.py`-скрипт с `requests`; для вывода результатов — писать в файл (`encoding="utf-8"`) и читать через Read, не через bash-консоль напрямую
- В dev-БД были рассинхронизированы sequence `organizations_id_seq`/`users_id_seq` (после ручного сидирования без `setval`) — почищено 04.07, но если где-то ещё заводили записи с явным id вручную, тот же класс бага может всплыть
- **Railway CLI установлен и залинкован** (`railway status`, `railway metrics`, `railway logs`) — можно диагностировать прод прямо из терминала без захода в dashboard. Подробности — [[architecture/deploy_ops]]

## Ссылки

- Финансовый эксперт → `D:\aispace\personal\knowledge\finance\nb1_cfo_operations\`
- Квитанции → `media/receipts/`
- Отчёт OpenRouter → `python scripts/or_report.py`
- Идеи "на подумать" (не для немедленной реализации) → `context/ideas.md`
