# Бот в группе «Жемчужина» — проект (18.09.2026, до реализации)

> Полный проект с примерами реплик — `context/revision/09_bot.md`. Здесь —
> решения, которые останутся верными после реализации.

## Зачем

Зазор между «кинули чек в WhatsApp» и «внесли на ноутбуке» — место всех дыр
сентября (двойные чеки и снятия, перевнесение чужим карманом, деньги на
руках без записи, три дня без листа кухни). Бот убирает зазор: сообщение в
группе становится событием системы сразу, и система сама знает, что должно
быть внесено.

## Решения владельца

- Группа одна, Telegram «Жемчужина». Махабат кидает туда все чеки и всю
  информацию о деньгах; WhatsApp для этого закрывается.
- **Автоматом всё, что можно.** Владелец — проектировщик, не оператор:
  обычные записи ложатся сразу под именем приславшего; ему уходит только
  подозрительное (дубль, минус кармана, неизвестное имя, дата старше трёх
  дней, необычная сумма, неуверенность модели, расход со счёта).
- **Конец дня — два числа вслепую:** каждому держателю «сколько на руках?»,
  Мунаре ещё «сколько на счету?». Бот свою цифру заранее не показывает.
  Ответ — пересчёт / сверка сразу; разница объясняется тем, что ждёт «да»,
  или одним вопросом кнопками, где «не знаю» — тоже ответ.
- **Утро владельца:** остатки, вчерашнее, «ждёт да», «не сошлось»; ответы
  словами; каждый разбор оставляет правило в Настройках (алиас, имя, порог,
  «этого поставщика не спрашивать»). Цель — четыре рабочих дня до состояния
  «утром сошлось, разбор — минуты».
- Суммы по людям (карманы, зарплата) — только в личку, не в группу.

## Принципы поведения

1. Молчит, пока не о деньгах.
2. Не спрашивает то, что может узнать сам (поставщик и дата с чека, карман —
   держатель наличных, счёт — по объекту человека).
3. Один вопрос за раз, ответ кнопкой; без ответа не повторяет, кладёт в
   вечерний итог и в «Ждёт вас».
4. Кому в карман: деньги у того, о ком сказано; если не сказано — у того, кто
   пишет.

## Сверка чека

Три ключа по убыванию надёжности: отпечаток фото → поставщик + дата + сумма
(±1 %) → сумма + дата. Рукописные товарные чеки читаются по сумме и магазину,
позиции остаются за Махабат.

## Факт на 18.09

Бот собран 15.09 (`app/services/bot.py`), но ни разу не работал: токена и
номера группы на Railway нет, Telegram ни у кого не привязан, в группе он
только говорит. Оживление — час без нового кода, но требует токена от
владельца.

## Черновики вместо автозаписи (решение владельца 21.09)

Отменяет «обычное пишется сразу» из проекта 18.09. Всё, что пришло в чат
«Жемчужина» или боту в личку, — **черновик** (строка `receipts`: kind
receipt | kitchen | service, source chat | private, payload — что распознано,
result_type/result_id — во что превратился, reject_reason). Черновик не трогает
деньги и склад. Махабат открывает его **той же формой, что вносит руками**
(Купили, Лист кухни, Расход без чека), уже заполненной, с фото справа:
номера строк, отмеченные подсвечены, «К следующей», полоса «Осталось…», одна
кнопка «Внести», «Не вносить» с причиной. Владелец видит копию каждой проводки
(нужен Start у бота — иначе Telegram 403). Модуль: `context/revision/11_bot_inbox.md`.

Что узнали на живых фото 21.09:
- Вид бот определяет по содержимому: цены и итог — чек; только количества и
  единицы, небольшие — лист кухни; крупные (мешки, десятки кг) — пересчёт.
  Подпись «кухня» нужна только при сомнении.
- Рукописный лист кухни распознаётся примерно на 3/4; остальное — вопросы в
  строках, для этого и нужна проверка человеком.
- Строки разбираются **сразу после прихода фото** (фоном после ответа
  Telegram + раз в минуту), не при открытии: иначе ожидание на экране.
- Дубль — только если по фото уже есть черновик/чек (отпечаток файла или
  file_unique_id Telegram), а не «бот уже видел»: иначе пересланное заново фото
  не становится черновиком.
- Дата на листе, прочитанная как будущая («16.10» вместо 16.09), отбрасывается —
  подсказка «день на листе не прочитан», а не «дня нет».
- «Расход без чека» = покупка без receipt_id; черновик услуги к ней не
  привязываем через чек (сломалось бы «Поправить»), фото — через черновик.
- Пятничный «у вас на руках X?» с автозаписью выключен: ритуал пересчёта и
  запись мимо проверки.

## See Also

- [[architecture/reconciliation]] — почему ежедневная сверка надёжнее правил
- [[architecture/new_entry]] — бот в новом входе как есть
- [[stakeholders/roles]] — кто что присылает
