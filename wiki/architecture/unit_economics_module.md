# Сотрудники / ФОТ / Амортизация — Архитектура и решения

> Реализовано: 10.07.2026. Blueprint (проектирование ДО кода, полная
> история решений с датами) — [[blueprints/unit_economics_module]].

## Зачем

Полная картина расходов для юнит-экономики требует ФОТ и амортизации, не
только питания/хозтоваров/закупа (см. `project_brief.md`, «Стратегическое
видение — продолжение 08.07»).

## Стратегическое решение — пересмотр Pearl vs 1С

Решение «закрыто 18.06» (Зарплата/Амортизация — целиком зона 1С) частично
пересмотрено: Pearl считает эти величины сам, **управленческой оценкой**
(не официальный бухучёт — НДФЛ/Соцфонд/проводки остаются зоной
бухгалтера/1С-аналога). Причина — не привязывать продукт структурно к
наличию 1С у клиента (цель на будущее — единый софт для садиков, не все
его имеют).

## Модель данных

### Employee
```
id, organization_id, full_name, role, salary,
status (active|terminated), created_by, created_at
```
Статус — отметка, не удаление (тот же паттерн, что `Student`
active/archived) — история не теряется при увольнении. Отпуск отдельного
статуса не требует, зарплата в отпуске всё равно платится.

### Asset.useful_life_months (расширение)
```
+ useful_life_months  Integer, nullable
```
`NULL` = не амортизируется. Предзаполняется по умолчанию из
`DEFAULT_USEFUL_LIFE_MONTHS` (категория → срок), можно поправить вручную.

Миграция: `e5f6g7h8i9j0`.

## ФОТ и Амортизация — НЕ Transaction, считаются на лету

**Важное архитектурное решение, отклонившее исходный план.** Первый вариант
блюпринта предлагал генерировать эти величины как `Transaction` тем же
паттерном, что `generate_monthly_charges()` (идемпотентно раз в месяц).
Отклонено после разбора с эдвайзером:

- Список детей для `Charge` уже заполнен на момент первого захода — паттерн
  безопасен. Список сотрудников стартует **пустым** — тот же паттерн
  заморозил бы ФОТ первого месяца на 0 навсегда (идемпотентность не
  позволила бы перегенерировать после того, как сотрудников добавили)
- Амортизация — не «текущая сумма», а ряд по времени (стартует через месяц
  после покупки, заканчивается когда актив самортизирован) — не подходит
  под «сгенерировать раз в месяц»
- Синтетические `Transaction` без поставщика/чека исказили бы счётчики
  долга поставщику и «без категории», рассчитанные на реальные закупки

**Решение:** обе величины — чистые функции (`app/services/unit_economics.py`),
считаются заново при каждом обращении, нигде не хранятся:

```python
monthly_payroll(db, org_id)       # SUM(Employee.salary WHERE status=active)
monthly_amortization(db, org_id)  # SUM по активам: cost/useful_life_months,
                                   # с обрезкой по накопленной сумме
```

Показываются на `/employees/` и `/assets/` (итог сверху списка), в
`/expenses/`/Закуп не попадают — это не покупки. Категории расходов
«Зарплата»/«Амортизация» не заводились — не нужны.

**Проверено эмпирически (10.07):** добавление сотрудника → ФОТ вырос;
увольнение → ФОТ сразу упал до 0 (не «застрял» бы, как случилось бы с
Transaction-подходом). Актив с датой покупки 6 месяцев назад,
60 000 сом / 60 мес. → амортизация 1 000 сом/мес., совпало с расчётом
вручную.

## Доступ — роль `founder`

Новая роль, не переиспользует `role=owner` (тот — технический доступ
Абдусаттара, `seed.py` id=1). Заведена для **Айдай** (реальный
бизнес-владелец) — `scripts/add_user.py`, пароль `004` (по аналогии с
000/001/002/003).

- `get_accessible_orgs()` — `founder` в одной группе с `owner`/`director`,
  полный доступ ко всем организациям
- `/employees/` — доступ только `owner`/`founder`. Не Мунаре/Махабат/Айжан
  — оклады чувствительнее, чем остальные данные, к которым у них уже есть
  доступ. Проверено: Айжан (`role=director`) редиректится с `/employees/`

## Экраны

- **`/employees/`** — итог «ФОТ в этом месяце» сверху, форма добавления,
  список (ФИО/роль/оклад/статус). Карточка `/employees/{id}/edit` — тот же
  паттерн просмотр→«Изменить»→«Сохранить» с подтверждением, что на карточке
  ребёнка (нетехнический пользователь не должен бояться случайно всё
  сломать)
- **`/assets/`** — расширена: поле «Срок службы» с плейсхолдером по
  умолчанию (JS меняется при выборе категории), колонка «Амортизация/мес.»
  в таблице, итог «Амортизация в этом месяце» сверху

## Не в V1

- НДФЛ/Соцфонд/удержания — никогда не в Pearl
- История изменений оклада — не версионируется, ФОТ «на сегодня» точный, ФОТ
  «на дату в прошлом» посчитать нельзя (нет истории)
- Тарифный лимит на вычет рекламных расходов по НК КР — отдельный вопрос
  финэксперту, не блокирует

## Новые статьи расходов (отдельно от ФОТ/Амортизации)

Коммуналка (+ Электричество/Вода/Отопление/Интернет), Охрана, Реклама —
решены 10.07, заведены как `ExpenseCategory` 05.08 (миграция
`b2e5e302718c`). Аренда — не нужна, помещение в собственности.

## Дашборд `/dashboard/unit-economics` (05.08)

Единая страница для собственников и Махабат (доступ owner/founder/staff —
тот же круг, что `/employees/`): питание, ФОТ, амортизация, коммуналка/
охрана/реклама, начислено vs оплачено за месяц. Каждый блок — с явным
индикатором ✅ посчитано / ⚠️ не хватает данных, а не молчаливый ноль —
принцип по прямому запросу владельца («если не хватает данных, чтобы
тоже показывалось»).

### Общая кухня Школы и Садика Сокулук — `kitchen_group()`

Реальный физический факт (подтверждено владельцем 05.08): один повар,
один закуп, одна столовая на Школу и Садик Сокулук. Это ломает наивное
«считать питание по одному org_id»:

- Приход товара (`WarehouseReceipt`) реально проводится под ЛЮБЫМ из
  двух id — на проде 11 приходов на 14 903 сом оказались под «Школой»,
  35 приходов — под «Садиком». Если считать среднюю цену только по
  одному org_id, часть товара получает цену 0 в расчёте стоимости
  списания — не гипотетический риск, реально произошло.
- Списание (`WriteOff`) сейчас **всегда** идёт под Садик Сокулук — у
  Школы пока нет своих `Group`/детей в системе (пилот), формула в
  `writeoff_calc.compute_day_draft` уже умеет считать `headcount["class"]`
  отдельно от `headcount["kindergarten_group"]`, но пока класс всегда 0.

Решение — `unit_economics.kitchen_group(org_id)`: для {2, 4}
(Школа, Садик Сокулук) возвращает оба id разом, для всех остальных
(Садик Кожомкул — отдельная кухня) — только сам id. Используется и для
питания (`monthly_food_cost`, средняя цена и сумма списаний), и для
Коммуналки/Охраны (`monthly_expense_total`) — та же логика: право
провести транзакцию под конкретным org_id зависит от роли (Махабат/
пилот-юзеры видят только Садик Сокулук, Айжан — только Школу), так что
реальный счёт может уйти под любой из двух id, разделять их при подсчёте
означало бы терять часть расходов.

**Пойман advisor-ревью до полного деплоя**: первая версия подписи на
экране гласила «сумма по обоим объектам вместе» — неточно, реально это
100% потребление Садика (headcount школы = 0), общий там только закуп/
цена. Переформулировано + добавлено предупреждение под «Итого расходов»:
если смотреть Школу и Садик по очереди и сложить два «Итого», общие
статьи (Питание/Коммуналка/Охрана) посчитаются дважды.

**ФОТ сознательно НЕ пулится через `kitchen_group`** — повара общие,
воспитатели/учителя нет. Привязка зарплаты — на уровне сотрудника
(`Employee.organization_id`), не общий котёл по паре объектов.

### Взвешенный сплит садик/школа — на будущее, заблокирован не формулой

Обсуждали (05.08) поделить общие статьи (питание, возможно коммуналку)
между Школой и Садиком по формуле `(headcount_садик × 0.8) : (headcount_
школа × 1.0)` — коэффициент 0.8 уже есть как `Organization.
sadik_portion_percent`, это не пробел. Реальный блокер — headcount школы
физически негде взять: в системе 0 групп/детей Школы. Формула та же,
что уже реально считается внутри `writeoff_calc.compute_day_draft` для
объединённого списания — просто её результат сейчас не разложен обратно
по объектам.

## Онбординг Школы — не начат, реальные технические пробелы (05.08)

При разборе того, что нужно для заведения Школы в систему целиком
(группы-классы, дети, явка, тариф), нашлось: **создание `Group` нигде в
коде не реализовано** — ни для одного объекта, включая Садик Сокулук
(его 6 существующих групп заведены напрямую в БД, не через форму).
`Service`/тариф для Школы (`organization_id=2`) — 0 записей. Явка
(`Attendance`) уже общая для любого org, доп.кода не требует.

Решено с пользователем: классы — с литерами (1А/1Б, не «1 класс»), поля
карточки ребёнка — те же, что у Садика (ФИО/ПИН), тариф пока единый на
всю Школу (не по классам). Группы для Школы заводятся так же, как и для
Садика когда-то — прямой миграцией по факту готового списка классов, не
отдельным CRUD-экраном (меняется примерно раз в год, строить UI ради
этого признано избыточным). Статус ожидания данных — файл памяти
`project_school_onboarding` (не здесь, там актуальнее по датам).

## Сроки полезного использования по умолчанию (ответ финэксперта, 10.07)

| Категория (`Asset.category`) | Срок | Обоснование |
|---|---|---|
| мебель | 60 мес. | Группа 2 НК КР (25%) |
| оборудование | 36 мес. | Бытовая техника, консервативный дефолт |
| игровой инвентарь | 24 мес. | Мелкий инвентарь/игрушки, Группа 1 (30%) |
| прочее | не амортизируется | Требует ручного ввода срока |

## See Also

- [[architecture/billing_module]] — скидка родителям за отсутствие
  (соседнее решение того же дня, не реализовано)
- [[architecture/expense_categories]] — полная структура категорий
