# Модуль Меню/приёмы пищи — Архитектура и решения

> Реализовано: 10.07.2026. Blueprint (проектирование ДО кода) —
> [[blueprints/menu_module]], там же полная история решений с датами.

## Зачем

Списание со склада на приём пищи (`WriteOff.meal_type`, готово с 09.07) не
было привязано к конкретному блюду — нельзя было посчитать себестоимость на
ребёнка, не было базы для будущего сравнения ожидание/факт (см. гап-анализ
в [[architecture/warehouse_module]]).

**Почему `dish_id` на списании:** цепочка Меню (блюдо на приём пищи) →
Списание (продукты, привязанные к блюду) → Посещаемость (число детей) даёт
«сколько продукта уходит на одного ребёнка для конкретного блюда» —
рецептура снизу вверх, из фактических данных, без ввода норм заранее. Сам
расчёт/отчёт по рецептуре — будущая фича, схема закладывалась уже сейчас,
чтобы данные начали копиться с первого дня.

## Модель данных

### Dish
```
id, name (unique), created_at
```
Глобальный каталог (не org-scoped), как `Product`. Только название, без
состава/граммовок.

### MenuEntry
```
id, organization_id, date, meal_type, dish_id (FK Dish),
created_by, created_at
```
4 приёма пищи — Завтрак/Обед/Полдник/Ужин (подтверждено Мунарой). Без
уникального ограничения на `(organization_id, date, meal_type)` — обед
обычно несколько блюд (суп/второе/компот), каждое отдельной строкой.

### WriteOff.dish_id (расширение)
```
+ dish_id  Integer, ForeignKey("dishes.id"), nullable=True
```
Nullable осознанно — списание проходит и без выбранного блюда (мягкое
предупреждение, не блок, см. ниже).

Миграция: `d4e5f6g7h8i9`.

## Защита от опечаток

`get_or_create_dish()` (`app/services/dishes.py`) — та же логика, что у
товаров (`get_or_create_product`): точное совпадение → нечёткий поиск
(rapidfuzz, порог 85) среди существующих блюд → и только если ничего
похожего не нашлось, создаётся новая запись. Без `DishAlias` — каталог
блюд маленький, фаззи-поиска по имени достаточно.

## Экраны

### `/menu/` — карточки по дням

Не широкая таблица (плохо ложится на телефон), а **карточка на каждый
день** (Пн–Пт), внутри — 4 блока по приёмам пищи. В каждом блоке:

1. **Чипы «обычно даём»** — блюда, чаще всего встречавшиеся в `MenuEntry`
   для этого `meal_type` (`frequent_dishes()`, топ-8 по частоте). Клик —
   добавляет без печатания. Формируется само по мере накопления истории —
   первую неделю почти пусто, дальше наполняется.
2. **Уже выбранные блюда** — чипы с крестиком для удаления.
3. **Поле с автокомплитом** — печатать новое блюдо, Enter добавляет чип
   (или клик по подсказке из `/menu/dishes/search`).

`GET /menu/?org_id=&start=&weeks=` — по умолчанию открывает ближайший
понедельник, `weeks` (1–4) расширяет диапазон, кнопка «+ Добавить ещё
неделю» просто увеличивает `weeks` в URL. `POST /menu/` пересохраняет весь
диапазон целиком (удаляет старые `MenuEntry` за эти даты, вставляет
заново по пришедшим тройкам `entry_date`/`entry_meal`/`entry_dish`) —
проще и предсказуемее частичного upsert.

**Предупреждение, не блок:** баннер наверху страницы, если у ближайшего
будущего буднего дня нет ни одной `MenuEntry`. Тот же принцип, что уже
применён в актуализации склада.

### `/warehouse/writeoff/meal` — выбор блюда на списании

Каждая строка товара получила необязательный `<select>` «Блюдо» —
подгружается через `GET /menu/dishes/for-meal?org_id=&date=&meal_type=`
при смене даты/приёма пищи вверху формы. Если меню на эту дату не
заполнено — список пуст, показывается предупреждение
(`#no-menu-hint`), но списание **не блокируется** — `dish_id` остаётся
`NULL`.

## Кто вводит

Тот же человек, что ведёт закуп/склад — в Сокулуке это **Махабат**
(`role=staff`), не отдельная роль. Меню на понедельник должно быть готово
не позже пятницы предыдущей недели (организационное правило, не
enforced в коде).

## Не в V1 (сознательно)

- Состав/граммовки блюда, прогноз ожидаемого расхода
- UI для родителей/бота
- Задним числом привязка старых `WriteOff` к блюду
- «Скопировать меню с прошлой недели» одной кнопкой — снято чипами
  «обычно даём», которые решают ту же задачу проще
- Варианты меню по сезону/дню недели — сад работает одинаково Пн–Пт

## Открытый вопрос

Скидка родителям за дни отсутствия (не про меню, но соседний вопрос) —
решена 10.07 (минус 100 сом/день), реализация в биллинге ещё не
спроектирована — [[architecture/billing_module]].

## Баг: чипы-подсказки не кликались (найден 15.07, подтверждён и задеплоен 16.07)

`onclick="addChip(...,{{ d.name|tojson }})"` внутри HTML-атрибута,
ограниченного двойными кавычками — `tojson` сам оборачивает строку в `"`,
что обрывало атрибут раньше времени и ломало клик для **любого** блюда
(не зависело от конкретного названия). Правило на будущее: `|tojson`
безопасен только внутри `<script>` (присваивание JS-переменной), не
внутри инлайновых HTML-атрибутов вида `onclick="..."` — там нужно
`data-*` атрибуты (штатное Jinja-экранирование `{{ }}`) +
`addEventListener` в JS, как уже сделано у автокомплита в этом же файле
и в `writeoff_meal_form.html`. Проверено вживую в браузере (Playwright,
16.07) — клик по чипу добавляет блюдо, ошибок в консоли нет. Задеплоено.

## Режим просмотра/редактирования по дням (согласовано 15.07, реализовано 16.07)

`/menu/` переведён на тот же паттерн, что уже есть у Расходов/карточки
ребёнка/Посещаемости (просмотр→«Изменить»→правка): день с уже сохранённым
меню (`day.has_entries` в `app/routers/menu.py`) открывается read-only
(список блюд текстом по приёмам пищи) с кнопкой «✎ Редактировать»; пустой
день — сразу в редактируемом виде без доп. клика. Переключение через
`toggleDay(dayId, editing)` в `menu/form.html` — по одному состоянию на
каждый день (не общий toggleBlock, как на других страницах, т.к. на
странице сразу несколько дней). Общая кнопка «Сохранить меню» одна на
весь диапазон — скрытые поля чипов остаются в DOM независимо от
view/edit состояния дня, так что просмотровые дни не теряют данные при
сохранении. Проверено вживую (Playwright): переключение в edit сохраняет
уже выбранное блюдо.

## Три реальных бага (23.07) — найдены по живым отчётам Абдусаттара, все задеплоены

**1. Fuzzy-match `get_or_create_dish()` ложно сливал блюда — защита от опечаток
работала только в одну сторону.** Порог `FUZZY_AUTO_MATCH=85` на `WRatio`
переключается на partial-ratio при сильном расхождении длин **в любую
сторону** — не только «длинный текст против короткого блюда», но и наоборот.
Реальный кейс: «Рыбьи биточки на пару» (21 симв.) против уже существующего
«. Овсяная каша на молоке со сливочным маслом» (44 симв., с лишней точкой —
видимо, старый артефакт OCR/опечатки) дало score 85.5 — блюдо Махабат
молча подменялось овсянкой. Первый фикс сессии проверял длину только
одним направлением (кандидат не короче 60% ввода) и пропустил этот
зеркальный случай. Финальная защита — симметричная: `min(len_a, len_b) >=
0.6 * max(len_a, len_b)`. Оба направления проверены на реальных строках
(включая regression на настоящих опечатках — "Каша ристовая"→"каша
рисовая", "Булочька"→"Булочка" — короткие расхождения по-прежнему
матчатся, не задеты).

**Урок:** при защите от одного направления bias'а fuzzy-matching —
всегда проверять симметричность вручную на реальных строках, не
полагаться на интуицию «длинное не должно матчиться на короткое» (в обе
стороны работает одинаково).

**2. Гонка автокомплита.** Debounce-запрос `/menu/dishes/search` (150мс)
не отменялся при нажатии Enter — если ответ приходил уже после того, как
поле очищено и чип добавлен, `render()` всё равно показывал дропдаун с
устаревшей подсказкой на уже добавленное блюдо. Случайный клик рядом
(следующий ввод) добавлял дубликат. Фикс: `render(query, items)` сверяет
`query` с текущим `input.value.trim()` перед показом — ответ на неактуальный
запрос молча игнорируется. Плюс `clearTimeout(timer)` при Enter/выборе
подсказки, чтобы не тратить лишний запрос.

**3. Гонка сохранения дня — настоящая причина большинства дублей на
проде.** Autosave шлёт `POST /menu/day` на каждое добавление блюда, без
очереди. Если Махабат быстро правит разные приёмы пищи одного дня подряд,
уходит несколько overlapping запросов на одну дату; `menu_day_save()`
делает «удалить всё за день → вставить заново» без какой-либо
сериализации — оба запроса видят «нечего удалять» и **оба** вставляют
свою копию, задваивая весь день (не одно блюдо — весь список каждого
приёма пищи целиком). Подтверждено детерминированно: 5 параллельных
запросов без защиты → 5 копий каждой строки; с фиксом — ровно 1 копия
(тест с искусственной задержкой между delete и insert, чтобы гарантированно
поймать окно гонки, а не полагаться на удачное совпадение по времени).

Фикс — `pg_advisory_xact_lock(org_id, date_as_int)` в начале
`menu_day_save()`, до `DELETE`. Держится до конца транзакции (до
`db.commit()`), снимается сам. Разные дни/объекты друг друга не блокируют
(разные ключи лока).

**Диагностика бага №1 и №3 велась через реальные прод-данные**, не
гипотетически: `/menu/dishes/search` — уже публичный HTTP-эндпоинт,
позволяющий проверять fuzzy-совпадения на настоящем каталоге блюд без
прямого доступа к БД (доступ к прямому Postgres прод-соединению из этой
сессии заблокирован классификатором auto-режима — см.
[[architecture/deploy_ops]]). Полная выгрузка всех недель меню (через
залогиненную сессию + regex-парсинг HTML) вскрыла реальный паттерн
дублей — **целые списки приёмов пищи повторялись** (не единичные
блюда), что и указало на гонку сохранения, а не на автокомплит.

**Данные почищены одноразовым скриптом** (20/23/27.07) через тот же
`/menu/day` эндпоинт, что использует форма — не прямой SQL. «Рыбьи
биточки на пару» не восстановлены: блюдо никогда не сохранялось под
своим именем, Махабат ввела его заново после фикса.

## Рецепт-каталог блюд и разворот на «рецепт прежде чем в меню» (29.07)

**Причина разворота:** свободный ввод названия блюда прямо в Меню
(`get_or_create_dish()`, описан выше) был главным источником бардака в
каталоге — опечатки и переформулировки создавали фантомные дубли без
рецепта, а блюдо без рецепта означало, что авто-списание по тех.карте
(см. [[architecture/warehouse_module]]) молча даёт 0 расхода — никто не
узнавал об этом, пока кто-то не сверял остатки склада вручную. Разбор
реальных прод-данных 29.07 показал масштаб: 106 блюд в каталоге, 24 без
рецепта, часть из них — задвоенные варианты одного и того же блюда с
**разными** цифрами в рецепте (например, «Кекс домашний морковный» и
«Морковный кекс домашний» — тот же рецепт, но масло сливочное 5/8г vs
4/6г; «Какао на молоке» и «Какао с молоком» — молоко 100/120г vs 20/30г,
разница в 5 раз, похоже на ошибку транскрипции тех.карты).

**Найденный попутный баг:** фаззи-сравнение в `search_dishes()`
(`app/services/dishes.py`) сравнивало `raw`/`remaining` как есть, без
приведения к одному регистру — опечатка с другим регистром («чй» при
реальном «Чай») получала заниженный балл (40% вместо 80% при правильном
сравнении в нижнем регистре) и проваливалась ниже порога автослияния.
Это объясняет часть того, почему дубли вообще создавались, а не
матчились. Починено — сравнение всегда в нижнем регистре, возвращается
оригинальное (не lowercased) имя.

### Каталог блюд — новые экраны (`/menu/dishes/*`, всё в `app/routers/menu.py`)

- **`/menu/dishes/`** — список всех блюд, статус рецепта (✓ есть / ⚠ нет),
  поиск, «+ Новое блюдо» (создаёт пустую карточку через тот же
  `get_or_create_dish()`, но это теперь единственный путь свободного
  ввода — раньше того же можно было добиться и через Меню).
- **`/menu/dishes/{id}`** — просмотр/правка рецепта: паттерн
  просмотр→«✎ Редактировать»→правка (как у карточки ребёнка/Расходов),
  пустой рецепт сразу открывается в редактировании без лишнего клика.
  Автокомплит товара переиспользует `/expenses/products/search`
  (`rank_candidates`), грамм — отдельными полями садик/школа. Полная
  замена рецепта на сохранении (delete+insert), не построчный diff — тот
  же приём, что и в `menu_day_save`. Валидация: строка без единого
  распознанного числа (обе колонки пустые или нераспознанный текст вроде
  «40г») не сохраняется молча — явная ошибка с сохранением введённого.
- **Переименование** — прямо на `/menu/dishes/{id}`, с защитой от
  переименования в уже занятое имя (подсказывает слить вместо
  переименования).
- **Удаление** — **одна кнопка**, без деления «безопасно» vs «полностью»
  (по прямому решению Абдусаттара 29.07 — задача была дать Мунаре чистить
  каталог без раздумий, разделение путало). Удаляет блюдо всегда,
  каскадно: `MenuEntry` — удаляются (дни меню опустеют, пересобираются
  вручную), `DishIngredient`/`DishMergeDismissed` — удаляются,
  `WriteOff.dish_id` — становится `NULL` (не удаляется — это факт склада,
  «что списали», не карточка блюда).

### `/menu/dishes/duplicates` — слияние похожих блюд

Кандидаты — все пары каталога с `token_sort_ratio ≥ 60` (ниже, чем
`FUZZY_THRESHOLD=72` у автокомплита — тут ревью человеком, можно закинуть
шире сеть), исключая пары, которые Мунара явно пометила «это разные
блюда» (`DishMergeDismissed`, миграция `t9u0v1w2x3y4`). **Только
предлагает — никогда не сливает молча**, тот же принцип, что и у
`rank_candidates` для товаров склада (фаззи-подбор ненадёжен даже на
эталонных данных, финальное слово всегда за человеком).

Слияние (`dish_duplicates_merge`) переносит **все** FK на `dishes.id` —
список проверен запросом к `information_schema`, не угадан:
`menu_entries.dish_id`, `write_offs.dish_id`, `dish_ingredients.dish_id`,
`dish_merge_dismissed.dish_id_a/b`. `MenuEntry` репойнтится с дедупом —
если на тот же `(org, дата, приём пищи)` уже есть запись survivor'а,
drop-строка удаляется, а не дублирует чип. Рецепт: если у survivor'а
рецепта нет, а у donor'а есть — переносится (не теряем единственный
существующий рецепт), иначе рецепт donor'а удаляется вместе с ним.

**Урок сессии**: первая версия слияния трогала только `MenuEntry` и
`DishIngredient` и падала 500-й на `WriteOff` (дерево использовалось на
`/warehouse/writeoff/meal`, не в Меню) и на собственной новой
`dish_merge_dismissed` (следующее слияние после первого дисмисса —
мёртвый FK). Найдено advisor'ом до деплоя, воспроизведено локально всеми
4 сценариями (WriteOff-конфликт, дедуп MenuEntry, dismissed-пара, попытка
удалить используемое блюдо) — только после этого задеплоено.

### Menu-day save — блюдо без рецепта больше нельзя добавить

`menu_day_save()` больше не вызывает `get_or_create_dish()` — форма шлёт
`dish_id` (не название), сервер проверяет, что у `dish_id` есть хотя бы
одна строка `DishIngredient`, иначе запись не сохраняется. Фронт
(`menu/form.html`) не даёт напечатать что попало — автокомплит фильтрует
по `only_with_recipe=1`, Enter добавляет только однозначное совпадение
(точное имя или единственный кандидат в свежей подсказке), иначе просит
выбрать из списка или сначала завести рецепт. Чипы «обычно даём»
(`frequent_dishes()`) убраны целиком вместе с функцией — с новым потоком
подсказки от каталога и так достаточно.

**Реальный баг, найденный advisor'ом и воспроизведённый до деплоя**:
`menu_day_save` по-прежнему делает «удалить все `MenuEntry` за день →
вставить заново из формы» (см. защиту от гонки выше) — а форма всегда
пересылает ВСЕ текущие чипы дня при любом автосохранении. Значит любое
добавление одного блюда с рецептом в тот же день молча стирало все уже
стоящие там блюда без рецепта (таких было немало — старые записи,
сделанные до 29.07, когда рецепт ещё не требовался). Фикс: перед delete
считывается множество `dish_id`, которые уже стояли в меню на эту дату
ДО сохранения (`already_had`) — при вставке для них рецепт не требуется,
для всех остальных (новые попытки добавить) — требуется. Это временная
бронь для старых данных, не лазейка для новых записей.

**Прод-очистка 29.07** (не осталось в истории меню, задокументировано
здесь ради контекста): 51 запись в `menu_entries` за 20.07–30.07 (включая
7 новых блюд, добавленных на 30.07, и «чай»/«хлеб»/«сухарики» и т.п. без
рецепта) удалены целиком по решению Абдусаттара — «тестовый период, сама
заведёт заново». Плюс 10 блюд без единого использования и без рецепта
(похоже, тестовый мусор с самого начала системы) удалены из каталога.

### Открытый вопрос

Часть каталога — однокомпонентные позиции вроде «Банан свежий»/«Порция
сыра»/«Вареное яйцо», которые не готовятся, но корректно списывают со
склада по норме на порцию (модель это позволяет — «блюдо» здесь означает
«позиция меню с нормой расхода», не обязательно готовку). Слово «Блюдо» в
интерфейсе может путать для таких позиций — переименование не сделано,
ждёт явного решения.

## Сессия 30–31.07 — импорт реальной тех.карты (4 недели), полная зачистка каталога

Махабат прислала «Исправленный меню 2026-2027.docx» (эталонный 4-недельный
ротационный список блюд, унифицированные названия) + 4 отдельных
тех.карты по неделям (`ТЕХ КАРТА 📅 1 НЕДЕЛЯ.docx` … `4 НЕДЕЛИ.docx`,
80 приёмов пищи, 195 упоминаний блюд, реальная граммовка садик/школа по
каждому ингредиенту). Заменили тестовый импорт 27.07 полностью.

### Архитектурное решение: садик = школа × коэффициент, не тех.карта

Тех.карта сама даёт садик/школу по каждому ингредиенту, но коэффициент
гуляет 0.75–0.90 по разным ингредиентам одного блюда (не константа) —
взвешивать это нет смысла: варится в одной кастрюле, порция садика физически
не может отличаться по составу, только по объёму. Решение Абдусаттара:
**школьная граммовка — единственный источник нормы**, `qty_sadik_g`
вычисляется как `qty_shkola_g × 0.80` при импорте (константа получена из
самих же ВЫХОД-строк тех.карты — каши/супы/пюре кластерятся в районе 0.80,
не выдумана). Штучная выпечка (булочка/пирожок/кекс/самса/курник/оромо и
т.п. — 21 блюдо) — исключение, `qty_sadik_g = qty_shkola_g`, одна и та же
порция независимо от возраста (список — в коммите импорта, там же где
считался коэффициент). Печенье (Овсяное/Песочное) в это исключение изначально
**не попало** (баг, найден Абдусаттаром при живой правке — «где мука
овсяная?» — оказалось это покупной снек без рецепта, а не выпечка), правится
вручную через `/menu/dishes/{id}` как тест самого механизма ручной коррекции,
не автоматикой.

### Полная зачистка вместо патча (подтверждено явно, не предположено)

Первая версия плана была surgical (переименовать/слить часть старых 34
блюд, оставить остальное) — Абдусаттар прямо потребовал полную зачистку:
удалить все 34 старых блюда + оставшиеся 16 `MenuEntry` (22–29.07),
завести 112 блюд с нуля из тех.карты. Проверено перед удалением: 0 строк
`write_offs` ссылались на текущие блюда — историю списаний терять было
нечему, зачистка безопасна. `write_offs.dish_id` для порядка всё равно
обнулён (не удалён), а не оставлен висеть на удалённых id.

### Резолюция ингредиентов — только «чистое», ничего не гадать

164 уникальных названия сырья из тех.карты сведены к 159 товарам склада
(151 было + 8 новых: Вермишель, Сахарная пудра, Повидло яблочное, Ядро
грецкого ореха, Зелень, Джем, Овсяное печенье, Песочное печенье — каждый
проверен фаззи-скорингом на отсутствие дубля в существующем каталоге,
максимальный найденный ложный кандидат 58%). Ручная alias-карта, не слепой
fuzzy — за сессию 27.07 fuzzy уже подводил на похожих случаях, здесь
поймано заранее на реальных примерах: «Крупа рисовая»/«Крупа пшенная»/
«Крупа гречневая»/«Крупа овсяная» fuzzy сводил не в те продукты каталога
(один раз даже пшеничная крупа вместо пшённой — разные крупы, легко
перепутать и человеку). Дровяной резерв: строки без единого числа
(«Джем плодово-ягодный» без указания вкуса, «Зелень» без указания травы)
осознанно НЕ резолвились в существующий похожий товар — заведены отдельными
явными позициями по прямому указанию Абдусаттара («завтра может быть яблочный
джем, не абрикосовый»; «зелень пучком на базаре, не кг»).

### Дедуп: два независимых слоя проверки, не один

Экран `/menu/dishes/duplicates` (29.07) фильтрует по имени
(`token_sort_ratio >= 60`) — этого недостаточно самого по себе: на свежем
импорте дал 58 кандидатов, из которых fuzzy-по-имени ложно предлагал
слить «Пшеничная каша»/«Пшенная кашa»/«Рисовая каша»/«Ячневая каша» (общий
шаблон фразы «... каша на молоке», разные крупы) — тот же класс ошибки,
что чинили в `search_dishes()` 29.07, только теперь на самом инструменте
дедупа. Настоящая проверка — сравнение по **(товар, граммовка)**, не по
названию: из 58 кандидатов только 6 оказались рецептно идентичны. Плюс
отдельно нашлись 4 реальных дубля с рецептом 1:1, которых экран вообще не
показывает — их сходство имени ниже порога 60% («Хлеб с маслом»/«Бутерброд
с маслом», «Ягодный морс»/«Компот из ягод» и т.п.). 52 ложных кандидата
отклонены (`dish_merge_dismissed`) пакетно по факту рецепта, не по имени.
**Практический вывод**: при любом будущем дедупе на этом каталоге — сначала
сверять по составу рецепта, потом по имени, никогда наоборот.

### Гейт «дубли перед меню» — сделан, смягчён в тот же день

Первая версия: `/menu/day` (форма и автосохранение) жёстко редиректила на
`/menu/dishes/duplicates`, пока очередь дублей не 0. Абдусаттар сам
переосмыслил вслух и отменил жёсткость: авто-списание считает каждое
блюдо по собственному `dish_id` независимо — дубль в каталоге не портит
цифры склада, только путает человека и дробит статистику «сколько раз
подавали». Финальная версия — мягкое предупреждение (баннер на
`menu/form.html` со ссылкой на `/menu/dishes/duplicates`), гейт из
блокирующего стал разовым по факту (сегодняшняя чистка), не постоянным
барьером на будущее. **Урок**: гейт «пока не 0» без ограничения по
времени — ловушка на будущее (новое блюдо с похожим именем молча
перезапирает экран посреди работы) — прежде чем реализовывать блокировку,
спросить, разовая она или постоянная.

### Найден и исправлен реальный баг: `is_standard=False` прячет товары от поиска

`/expenses/products/search` по умолчанию ищет только среди
`is_standard=True` — 18 реальных товаров (в т.ч. Соль, Чай чёрный, Лук
репчатый, Помидоры) были помечены `is_standard=False` с прошлых сессий и
оказались невидимы для автокомплита на экране рецепта. Пойман advisor'ом
на моменте «делаем фичу создания нового товара из рецепта» — без фикса
поиска фича начала бы плодить дубли ровно на самых частых товарах.
Добавлен параметр `standard_only` в `search_products()` (default true —
поведение квитанций/расходов не тронуто), экран рецепта явно просит
`standard_only=false`. Фича «+ создать товар прямо из рецепта» — план
согласован (категория+единица обязательны, серверная проверка на похожий
существующий товар перед созданием, is_standard=True сразу — раз человек
подтвердил), не реализована, первое дело следующей сессии.

### Защита от «тихого нуля» — распространена на привязку товара, не только на число

`dish_save()` (форма рецепта) 29.07 уже блокировала сохранение строки с
нераспознанным числом граммов. Тот же класс дыры нашёлся для привязки к
складу: `product_id = NULL` (ничего не выбрано из автокомплита) раньше
сохранялся молча — ингредиент просто выпадал из авто-списания без единого
сигнала. Теперь строка без выбранного товара тоже блокирует сохранение
всего рецепта (полная замена, не построчный diff — правка одного названия
в форме сбрасывает его `product_id`, следующая пересборка потребует
пере-выбора этой строки, даже если менялось только соседнее поле).

## Проверено (10.07, Playwright, локально)

- Создание меню на неделю, чипы, автокомплит, сохранение — 0 ошибок
  консоли, баннер-предупреждение корректно сдвигается на первый
  незаполненный день
- Повторное открытие страницы после сохранения — блюдо появляется и как
  сохранённый чип, и в «обычно даём» для этого приёма пищи
- Списание на приём пищи с привязкой к блюду — `WriteOff.dish_id`
  сохраняется корректно; без меню на дату — форма не блокируется
- `pytest` — без новых регрессий (те же 2 доеденных падения, не связанные
  с этим модулем)
