---
name: expenses-module
description: Архитектура и паттерны модуля Расходы — OCR, справочник продуктов, confirm flow
metadata:
  type: project
---

# Модуль Расходы — архитектура

## Поток данных

```
Фото → upload → Receipt (file_path, file_hash) → OCR → ReceiptItems
                                                        ↓
                                             confirm → Transaction
                                                        ↓
                                               ReceiptTransaction (связка)
```

## Ключевые решения

**file_hash (SHA256)** — защита от дублей. Если то же фото загружается повторно (бот + веб), находим существующий Receipt и переиспользуем.

**Фото на диске, путь в БД** — `receipts.file_path` = relative path. FastAPI отдаёт `media/` как статику. При переносе в облако: заменить запись файла на S3 upload в одном месте (`handle_upload`).

**Manual receipts** — ручные записи без фото создают Receipt с `file_path='manual'`, `ocr_status='manual'`. Это позволяет хранить позиции (ReceiptItems) без реального файла, не меняя схему.

---

## Справочник продуктов — архитектура (решено 27.06.2026)

### Два уровня продуктов

```
Product.is_standard = True   → эталонный (39 реальных продуктов из OCR-каталога)
Product.is_standard = False  → временный (создаётся автоматом из незнакомых OCR-строк)
```

**Почему `is_standard` флаг, а не `category IS NULL`:** авто-промоут не знает product.category в момент первого появления. Явный bool надёжнее.

**Эталонный каталог:** `data/standard_products.json` — 39 продуктов, реально встреченных в 101 квитанции. Загружается через `scripts/load_standard_catalog.py`. Категории: мясо, овощи, зелень, крупы, молочные, специи, масла, фрукты, хлеб.

**Временный продукт:** создаётся через `get_or_create_product()` с `is_standard=False`. Никогда не промоутируется автоматически для категорий Ремонт / Транспорт / Прочее.

### Авто-промоут при N=3

```python
# services/products.py — maybe_promote()
if COUNT(ReceiptItem WHERE product_id=X) >= 3:
    product.is_standard = True
```

Счётчик = живой COUNT из ReceiptItems, не хранится отдельно. Вызывается в `handle_confirm` только если категория чека eligible (не Ремонт/Транспорт/Прочее).

### Дедупликация OCR-опечаток

Gemini-матчер видит ОБА типа (стандартные и временные). "корианд" и "кореанд" → оба матчатся к одному временному продукту → счётчик растёт корректно. При промоуте alias сохраняется.

### Структура данных

```
products (name, category, unit, is_standard)
product_aliases (raw_text → product_id)
receipt_items.product_id → products
```

Старые ReceiptItems при промоуте не трогаем — они уже ссылаются на product_id. Когда продукт промоутируется (`is_standard=True`), старые записи автоматически выигрывают.

---

## Два уровня детализации расходов

| Тип чека | Пример | Что сохраняем |
|---|---|---|
| **Детальный** | Продукты питания | ReceiptItems + Products + WarehouseReceipt |
| **Агрегированный** | Ремонт, Транспорт, Прочее | Только Transaction (сумма + категория + дата) |

Категории-исключения для детализации: `{"ремонт", "транспорт", "прочее"}`. Определяется в `_is_promote_eligible()` в expenses.py.

---

## Четыре состояния на confirm-странице

| Состояние | Рамка | Хинт | Что значит |
|---|---|---|---|
| `product_matched` | Зелёная сплошная (2px) | ✓ эталон точно | Точный alias — эталонный продукт |
| `fuzzy_matched` | Жёлтая сплошная (2px) | ? эталон, проверь | AI нашёл эталонный, не 100% |
| `provisional_matched` | Оранжевая пунктирная (2px) | ~ временный, накапливается | AI нашёл временный продукт |
| ни одно | Красная сплошная (2px) | ✗ не найдено | Совсем незнакомо |

**Правило для Мунары:** рамка = нужен взгляд. Без рамки = всё ОК (зелёная) или совсем незнакомо (красная).

---

## Авто-определение категории расхода

Приоритет:
1. Уже есть транзакция для этого чека → берём её категорию
2. Есть распознанные эталонные продукты (`display_product_id` + `is_standard=True`) → "Продукты питания"
3. Нет эталонных → история прошлых чеков (cat_votes)

Ключевой код — `confirm_form` в `expenses.py`. Пересечение product.category (мясо/овощи/...) с `FOOD_PRODUCT_CATS` → ExpenseCategory "Продукты питания".

---

## Confirm flow

1. `GET /{id}/confirm` — рендер с позициями из OCR, нормализация через Gemini, авто-категория
2. Пользователь проверяет позиции (цветовые подсказки), категорию, сумму, дату
3. `POST /{id}/confirm` — создаёт Transaction(s), авто-промоут временных продуктов, WarehouseReceipt если warehouse_eligible
4. Redirect → экран успеха (та же страница в confirmed state)

**Split:** одна квитанция → несколько Transaction разных категорий через ReceiptTransaction.

---

## Workflow Мунары (ежедневный)

```
Фоткает чек → загружает в систему
    ↓
OCR читает позиции (2-5 сек)
    ↓
Страница подтверждения:
  Зелёная рамка → ничего не делает
  Жёлтая рамка → нажимает OK или правит
  Оранжевая рамка → нажимает OK (накапливается к промоуту)
  Красная рамка → выбирает из поиска или оставляет
    ↓
Категория проставлена автоматом → проверяет
    ↓
"Провести"
```

Для строй/ремонт чеков: позиции не показываются, только сумма + категория.

---

## Хранение данных (retention)

- Транзакции в БД: хранить 5 лет (закон КР)
- Фото файлы: можно удалять через 1-2 года после confirmed_at
- OCR текст: хранить вместе с транзакцией (мало места)
